# Волна 19 — единый интерфейс сущностей + закупочный контур + шов «движок ↔ вью»

**Статус:** Ф1/Ф9 закрыты и в проде; **Ф4, Ф10, Ф11 сделаны локально** (2026-07-24),
**Ф3 целиком — Ф3a + добор + Ф3b** (2026-07-25) и **Ф2 (пикер)** (2026-07-25, браузер
за Иваном). Единый интерфейс идентичности достроен **без исключений**:
`Item.design_item_id → code` был последним. **Ф12a (канон формы + пилот на Изделии и
Поставке) — СДЕЛАНА и ПРИНЯТА в браузере 2026-07-26** (шесть доборов по ходу приёмки;
`FormShell`, табы, мета-фраза, глифы строк, вложения). Остаток волны —
**Ф12b (владельцы вложений — backend), Ф12c (раскатка на 10 форм), Ф12d (чистка
вёрстки шапок)**, Ф5 (форма закупки),
Ф6 (Заказ → УПД), Ф7 (шов), Ф4b (xlsx-бланк). Волна **расширена** (2026-07-23): к консистентности
закупок и шву добавлена третья, самая широкая опора — **единый интерфейс идентичности
всех сущностей** (`code`/`description`). Волна сознательно сделана долгой и серьёзной.
Прод отвязан (живые данные стянуты снимком, на прод не катим до конца волны) — чистим
решительно и целиком.

**Контекст.** Plume в проде, пользователи работают, БД нагружена. Самая отработанная
часть — **Изделия**: type-ahead-пикер, статус-глиф, замок формы, чистая форма.
Волна 19 подтягивает к этому «стилю и настроению» закупочный контур
(Закупка → Заказ → УПД), закладывает **шов между движком и представлением** и —
шире всего — **сводит «ручки» всех сущностей к одному интерфейсу**: у каждой ровно
`code` (уникальный короткий жаргон) + `description` (внятное человеческое имя).

**Три цели волны, равноправные:**

1. **Стройность модели** — все сущности получают один интерфейс идентичности
   (`code`+`description`), расползшиеся синонимы ручек (`design_item_id`/`name`/`label`/
   `number`-как-имя) сводятся к одному; мёртвый `note` удаляется везде.
2. **Функциональная** — закупки/заказы/УПД становятся такими же зрелыми, как изделия.
3. **Архитектурная** — фронт перестаёт быть сросшимся с бэкендом: движок (Иван) даёт
   стабильный контракт, вью (кто угодно) строится поверх. Проверка шва — скелет
   второй темы («надувной», не-VS-Code).

---

## Принятые развилки (2026-07-20)

| # | Развилка | Решение |
|---|----------|---------|
| Р1 | `cancelled` у Закупки/Заказа | **Отмена = удаление**, как у ордеров с волны 13. Ось строго `{draft, posted}` |
| Р2 | Пеггинг: сколько заказов на проект под планом | **Несколько**, выбор конкретного заказа прямо в строке аккордеона |
| Р3 | Откуда поставщик при «Заказ → УПД» | **`Procurement.contractor`** — контрагент у Закупки (она и есть «один поток общения») |
| Р4 | Объём темизации в этой волне | **Шов + скелет второй темы** (оболочка + режим «Изделия») |

## Сквозное решение: одна ось статуса на весь продукт

Все сущности со статусом переезжают на **`DocStatus {draft, posted}`** — тот же enum,
что уже несут `StockDocument` и `Item` (волна 17). Одна ось → один глиф, один замок,
один набор правил, ноль дублирующего кода.

- Значения в БД — `draft` / `posted` (не заводим третий синоним `approved`: `Item`
  и шесть ордеров уже на `posted`, менять их ради слова дороже пользы).
- **Подписи — по сущности**: «Черновик» / «Утверждена» (Закупка), «Утверждён» (Заказ),
  «Проведён» (ордер), «Зафиксировано» (Изделие). Меняются в одном словаре.
- **Глиф — везде один**: ✓ зелёный (posted) / ○ оранжевый (draft), компонент
  `ItemStatusGlyph` переименовать в `StatusGlyph` и применить ко всем спискам.

**Что это ломает и как чиним — Ф1.**

---

## Сквозное решение №2: единый интерфейс идентичности (`code` + `description`)

**Принято 2026-07-23.** «Ручки» сущностей расползлись по двум осям, и одну и ту же
роль зовут по-разному:

| Роль | Как зовётся сейчас (россыпь) |
|---|---|
| **Ключ** (короткий, уникальный, человек читает и опознаёт) | `design_item_id` (Item), `code` (Project/Location/Category), `number` (документы), **нет** (Procurement/Purchase) |
| **Имя** (развёрнутое человеческое) | `description` (Item), `name` (Project/Location/Counterparty), `label` (Category), **нет** (документы) |

Причём `name` **перегружен**: у Project/Location это «имя-описание», у Counterparty —
сам ключ. Это и есть та расплывчатость, ради устранения которой затевается свёртка.

**Целевой интерфейс — у КАЖДОЙ сущности ровно пара:**

- **`code`** — уникальный короткий текстовый ключ-жаргон (напр. Item `BGB707…`,
  проект `ДО`, закупка `Нева ДЗЗ 1`). Уникален в пределах своего типа.
- **`description`** — внятное развёрнутое имя для человека (напр. «Датчик ориентации
  СУДН», «Закупка основных электронных компонентов для ДЗЗ в Неве»).

Иван осознанно принимает **небольшую расплывчатость самих слов** «код/описание» ради
**строгой единой пары** у каждой сущности: поля «работают» чуть по-разному в разных
местах, но называются и читаются одинаково.

### Карта свёртки по сущностям

| Сущность | Ключ → `code` | Имя → `description` | `note` |
|---|---|---|---|
| **Item** | `design_item_id` → `code` | `description` ✓ | — |
| **Project** | `code` ✓ | `name` → `description` | — |
| **Location** | `code` ✓ | `name` → `description` | — |
| **Category** | `code` ✓ | `label` → `description` | — |
| **Counterparty** | + `code` (напр. `КОМПЭЛ`) | `name` → `description` | — |
| **Procurement** | + `code` | + `description` | удалить |
| **Purchase** | + `code` | + `description` | удалить |
| **Документы** (Receipt/Transfer/Writeoff/Requisition/Inventory/Relocation/Kitting) | + `code` (напр. `Нева ДЗЗ 1`) | + `description` | удалить |

**Документы (решение Ивана 2026-07-23).** Свёртка проходит «без натяжки»: документы
получают **наши** `code`+`description` для консистентности со всеми — и **сохраняют**
`number` / `date` / `contractor` и прочие обязательные поля как **юридически значимые**
части документа. Роли честно разведены: `code` («Нева ДЗЗ 1») — наш ярлык, идёт в
списки; `number` — внешний номер накладной, **заполняем только когда с ней будут
проблемы**; `description` — развёрнутое имя. Так у **всех** есть `code`+`description`,
а документы просто богаче на юридические поля.

**`note` удаляется везде** (Procurement, Purchase, все StockDocument). Решение Ивана:
поле ни разу не пригодилось; понадобится — заведём заново без сожалений. Роль «вольной
ремарки» больше не занимает место в интерфейсе.

**Что даёт свёртка:**
- `name` как имя-поле **исчезает у всех** — перегрузка снята под корень.
- `Item.design_item_id → code` заодно **закрывает старую неловкость** (JOURNAL
  2026-07-12: имя `design_item_id` брали, чтобы не столкнуться с Django-аксессором
  `item_id` у FK; `code` этой коллизии не создаёт). Синк библиотеки маппит CSV-колонку
  «Design Item Id» → поле `code` (переименовывать саму библиотеку не обязательно).
- Списки, пикеры, шапки форм везде показывают одну пару — код + описание.

**Оговорка про Item — СНЯТА 2026-07-25.** `Item` (переименование `design_item_id →
code`) ехал **отдельным заходом** (Ф3: сперва оси `native`/`synced`/`locked` = Ф3a,
затем сам rename = Ф3b) — тот же файл модели и тот же синк, дробить их дважды по Item
было нерационально. Все прочие сущности свернулись в Ф10. **Итог: пара
`code`+`description` теперь у КАЖДОЙ сущности без исключений.**

---

## Ф1. Статусы: свести всё к `draft ⇄ posted`

**ЗАКРЫТА ПОЛНОСТЬЮ 2026-07-22.** Ф1a (ось `draft/posted`, прод) + Ф1c (bool `locked`,
прод) + Ф1b (глифы: единый `StatusGlyph` codicon lock/unlock, цвет = «как идут дела» —
фиксация у справочников, покрытие у Заказов, worst-of у Проектов; ось разбора на
`layers` в проекте; detail-мета сведены). Сюита 344 зелёная. Ниже — история фаз.

### Найденное в коде (обоснование)

- `Purchase.Status.PARTIAL` и `RECEIVED` — **мёртвые**: не присваиваются нигде
  (`engine.py`, `views.py`, `admin.py`), 0 упоминаний в тестах. Существуют только в
  enum и в README. «Получен / частично получен» — это **вычисляемая** величина из
  приходов, у неё уже есть источник: `_line_received` (`engine.py:416`).
- `Project.Status.DRAFT` — **мёртвый** (`create_project` сразу ставит `ACTIVE`).
- `Procurement.Status` и `Purchase.Status` — свои enum-ы, дублирующие `DocStatus`.

### Работы

**Ф1 разрезана на две части** (2026-07-20): отпечаток ~176 упоминаний в бэкенде и ~19
во фронте — за одну сессию честно не проходит. **Ф1a** = ось целиком в бэкенде + всё,
что иначе сломалось бы между коммитами. **Ф1b** = чисто аддитивное (глифы), ничего не
ломает, живёт отдельно.

#### Ф1a — ось (СДЕЛАНО 2026-07-20)

- [x] **Модель.** `Procurement.status` и `Purchase.status` → `DocStatus`.
      `Project.Status` → `{active, closed}` (убран мёртвый `draft`; это **жизненный
      цикл**, а не замок — осознанно не сведён к draft/posted). Обеим сущностям добавлен
      `is_posted` — тем же он и включил общий `_require_draft`.
- [x] **Миграция данных** `0003_status_single_axis`: `sent → posted`;
      `partial`/`received` → `posted`; **`cancelled` → `draft`**, НЕ удаляя автоматически.
      Печатает счётчики. **На реальных данных — no-op:** снимок прода 2026-07-20 показал
      1 закупку и 4 заказа, все `draft`, проекты все `active`. Ручной разбор, заложенный
      в план вслепую, не потребовался. Той же миграцией **снят призрак
      `0002_transfer_posted`** (см. реестр Ф8) — локально ушёл, **на проде долг ещё висит**.
- [x] **Движок.** `cancel_*`/`restore_*` снесены; `send_*`/`unsend_*` → `post_*`/`unpost_*`.
      Все `exclude(status=CANCELLED)` из счётов ушли вместе со статусом.
      `_require_purchase_draft`/`_require_procurement_draft` свёрнуты в общий
      `_require_draft(doc, msg)`; `_require_procurement_peggable` стал пустым и удалён.
- [x] **API.** `send/unsend/cancel/restore` → `post/unpost` (`DELETE` уже был).
- [x] **Фронт (только ломающее).** Ветки `cancelled`/`restore` убраны из
      `ProcurementView`/`PurchaseView`, клиент переведён на `post*`. Кнопки «Отменить»
      сняты: удаление живёт в шапке под замком, чтобы действия не двоились.
- [x] **README.** ER-диаграмма (`PROCUREMENT`/`PURCHASE`/`PROJECT`) + текст про закупки
      и про счётчик «заказано». Продуктовая диаграмма статусов не показывает — правки
      не требовала.
- [x] **Тесты.** Сюита 334 зелёная. Два теста отмены переписаны на новый канон
      «отмена = удаление» (утверждённый документ не удаляется, сперва снять замок).

#### Ф1c — строки статуса → `bool` (СДЕЛАНО 2026-07-21)

**Решение Ивана:** отказаться от строковых статусов совсем. Мотив — не экономия, а
**понятность модели**: «доверять или проверять», два состояния, которые не надо
запоминать словами. Цена не рассматривается как аргумент (см. JOURNAL 2026-07-20).

**Порядок важен: Ф1c идёт ПЕРЕД Ф1b.** Иначе общий `StatusGlyph` будет построен вокруг
`status: 'draft'|'posted'` и переделан сразу после. Словарный проход («Утвердить»)
сливается с Ф1b — обе про подписи.

**Именование закрыто (2026-07-20):**
- **Поле — `locked` на всех сущностях.** Один смысл: `true` = заперто и заморожено.
  Проявляется **сильнее у документов** (замок формы + арифметика: запертый заказ
  считается в «заказано», запертая комплектация рождает лот) и **слабее у Изделия и
  Проекта** (только заморозка, без арифметики). Обоснование, что это одна ось, а не
  две: «заперто = доверяем = считаем».
- **Слово в UI — «Зафиксировать» / «Расфиксировать»** на всех сущностях. Выбрано не
  как самое красивое, а как самое функциональное: общая шапка форм **уже** рендерит
  `🔒 зафиксирован` (`FormHeader.tsx`), то есть слово не вводится, а признаётся.
  «Запереть» отвергнуто из-за коллизии с замком формы (см. Ф9), «Утвердить» — из-за
  натяжки для Изделия и Комплектации.
- **Эндпойнты — `lock` / `unlock`** (вместо `post`/`unpost` из Ф1a).

- [x] **Модель** (namespace `DocStatus` удалён): `Item.status` → `locked`;
      `StockDocument.status` → `locked`; `Procurement.status` → `locked`;
      `Purchase.status` → `locked`; `Project.status` → `locked`.
- [x] **Проект: даты становятся чисто информационными.** `started_at` → `started`,
      `closed_at` → `closed` (суффикс `_at` убрать). **Ни с чем не связаны, ни на что
      не влияют** — Иван проставляет реальные сроки руками, чтобы знать, сколько работа
      заняла на самом деле, независимо от формальностей. Из `close_project`/
      `reopen_project` авто-простановку `closed_at` **убрать**. Коллизии имён с
      bool-полем нет именно потому, что замок называется `locked`, а не `closed`.
- [~] **Проект: `locked` хранимый, а не вычисляемый** — чтобы не гонять разузлование на
      каждый чих. Кнопка **«Проверить возможность закрытия»** пробегает по остаткам и
      разблокирует (или гасит) кнопку «Зафиксировать проект». У запертого проекта
      дефициты **не считаем вообще** — показываем шапку и выпущенные изделия.
- [x] **Миграция** `0004_locked_bool_axis`: добавить поле → залить (`locked = status == 'posted'`, для проекта
      `locked = status == 'closed'`) → снять `status`. Симметрично обратима.
      Объём: 172 изделия, 1 закупка, 3 заказа, 4 проекта, **0 ордеров** — дроп БД НЕ
      нужен, риск минимальный.
- [x] **Движок:** `_require_draft` → `_require_unlocked` (проверка `doc.locked`); `post_document`/
      `unpost_document` → `lock_document`/`unlock_document`. Свойство `is_posted`
      исчезает — 46 вызовов становятся прямым обращением к полю; 67 обращений к
      `DocStatus.` уходят.
- [x] **API:** `status: 'posted'` → `locked: true`; маршруты `/post/`,`/unpost/` →
      `/lock/`,`/unlock/`. Фронт и бэк едут **вместе** (урок Ф1a: иначе между коммитами
      UI дёргает несуществующее).
- [x] **Фронт:** сравнения со строкой + словари `PURCH_ST`/`PROC_ST`/`KIT_STATUS` → функции
      `purchaseLock`/`procurementLock`/`kitLabel`; `ItemStatusGlyph` принимает `locked`.
- [x] **README:** обе диаграммы + текст (`ITEM`, `STOCKDOCUMENT`, `PROCUREMENT`,
      `PURCHASE`, `PROJECT`) + новые имена дат Проекта. Заодно закрыт дрейф «`ITEM.status`
      отсутствует в диаграмме» (числился за Ф3) — поле `locked` теперь на месте.

**Сделано сверх плана (2026-07-21), осознанно:**
- **Единый глагол замка в API у ВСЕХ сущностей.** План просил переименовать только
  `post`/`unpost`. Но рядом жили `approve`/`unapprove` (поставка) и `close`/`reopen`
  (комплектация, проект) — три словаря на одну ось. Оставить их значило оставить ту же
  болезнь, ради которой волна и затевалась, поэтому все переехали на `lock`/`unlock`.
  Число понятий не выросло, а упало с трёх до одного.
- **Слой совместимости комплектации снят.** `_kitting_status_out` проецировал замок в
  исторические `wip`/`closed` «до фронт-среза». Фронт в этой сессии переехал вместе с
  бэком — третьего словаря статуса в API больше нет.
- **`Item.locked` добавлен в ER-диаграмму** (дрейф с волны 17, числился за Ф3).

**Клик по dev (Иван, 2026-07-21):** миграция накатана на снимок прода — Item 107/172,
Procurement 1/1, Purchase 0/3, Project 0/4, ордеров 0. Изделия, Закупка, Заказы —
**зелёные**. Проект — поля даты закрытия в форме пока нет (по плану, не дефект).
**Комплектация — плохо**, но она вне закупочного контура волны: отложена целиком
отдельным заходом, находка записана в [BACKLOG.md](BACKLOG.md) («Движок и модель»).
Осознанное решение — не открывать второй фронт.

**Осталось от Ф1c (перенесено):** кнопка «Проверить возможность закрытия» и отказ
считать дефициты у запертого проекта — это уже поведение вью, а не ось; поле `locked`
у Проекта хранимое и готово. Делаем вместе с Ф1b/Ф9.

**Что закрывается даром:**
- Долг «нет `CheckConstraint` на `status`» — `tinyint(1)` мусор принять не может;
  строку из реестра Ф8 убрать.
- Коллизия имён в API: `status` перестаёт значить два разных смысла и остаётся
  **только осью покрытия** (`to_order`/`on_order`/`available`). Ось дефицита при этом
  не трогаем вообще — она признана удачной.
- Миграции переименования исчезают навсегда: подписи живут во фронте, смена слова —
  правка строки, а не данных (сегодня за `sent → posted` заплатили миграцией).

**Вопрос «хранимый или вычисляемый» — ЗАКРЫТ:** `Project.locked` хранимый (детали в
списке работ выше). Вычисляемым он гонял бы разузлование на каждый чих; вместо этого —
кнопка «Проверить возможность закрытия», которая пробегает по остаткам и разблокирует
фиксацию. README при реализации сформулировать так, чтобы «заперт, когда всё сведено»
читалось как **следствие проверки**, а не как автоматика.

#### Ф1b — глифы (ПОСЛЕ Ф1c) — развилки решаются 2026-07-22

**Решения Ивана (2026-07-22):**
- **Ось фиксации = codicon `lock`/`unlock`.** Уходим от эмодзи И от ✓/○ полностью.
  `lock` — зафиксировано, `unlock` — расфиксировано. Строгая проекция `bool locked`
  прямо из БД, без синонимов. Компонент `ItemStatusGlyph` → `StatusGlyph(locked)`,
  раскатать на списки: Изделия, Компоненты, Проекты, Ордера (6 видов), Закупки, Заказы.
  (Склады — без глифа фиксации, у Location её нет.)
- **Ось покрытия (только Заказы) = ЦВЕТ, не отдельный глиф.** Глиф в строке заказа —
  фиксация (`lock`/`unlock`); его **цвет** кодирует покрытие лотами: **красный** — ни
  одного лота, **оранжевый** — часть пришла, **зелёный** — все позиции разрешились в
  лоты. Так две оси разведены (глиф ⟂ цвет) и `✓`-коллизия из старого плана снята.
  Агрегат покрытия — в боковом списке Заказов + шапка/строки детали (`purchase_cockpit`
  уже даёт `received`/`remaining`).
- ~~«Один глагол — Утвердить»~~ — **снято:** Ф1c уже свела всё к «Зафиксировать».

**Выбор Ивана (2026-07-22, по [макету](https://claude.ai/code/artifact/6c24c32c-c410-48dd-ad50-10c9290f2e88)): Б — замки с цветом.**
«Замки с цветом однозначно лучше серых».

**Сквозной язык цвета (принят 2026-07-22):**
- **Красный** — плохо: пустота, работа не идёт, «мяч на нашей стороне».
- **Оранжевый** — внимание: работа в процессе, следи/контролируй.
- **Зелёный** — хорошо: сделали, приехало, закрыли, расслабься.
- **Синий** — действия (кнопки, выделение строки списка). Не статус.

**Глиф = фиксация (codicon lock/unlock), цвет = «как идут дела» по сущности:**
- **Изделия/Компоненты/Ордера/Закупки:** цвет = фиксация. `lock`+зелёный (зафиксировано,
  «хорошо»), `unlock`+оранжевый (расфиксировано/черновик, «внимание»). ← **чистый фронт**,
  `locked` уже в каждой строке.
- **Заказы:** глиф = фиксация; **цвет = покрытие лотами** — красный (нет лотов) /
  оранжевый (часть) / зелёный (все позиции разрешились). ← нужен агрегат
  `_purchase_coverage` (движок), новое поле в `_purchase_row`.
- **Проекты:** добавить глиф замка (сейчас синий кружок). Проекция `locked` (вычисляемый,
  значит скорее «закрыт», чем «зафиксирован» — допущение принято): `unlock` = активный,
  `lock` = архивный. **Цвет = worst-of по проекту** — красный (что-то не заказано) /
  оранжевый (что-то не пришло/не доделано) / зелёный (всё готово, осталось закрыть оффлайн).
  ← нужен `_project_health` (движок гоняет дефицит на проект — **linza-масштаб, perf**).

**Раскладка на две части:**
- [x] **Ф1b-фронт (СДЕЛАНО 2026-07-22):** `ItemStatusGlyph` → `StatusGlyph(locked, tone, title)`
      на codicon `lock`/`unlock` (класс `.ci.sg` = 13px + тон). Раскатан на все списки:
      Изделия/Компоненты/Ордера/Закупки — цвет-фиксация (зелёный заперт / оранжевый открыт);
      Проекты (замок вместо синего кружка, `title` активный/архивный) и Заказы — глиф есть,
      цвет **нейтральный (`tone=none`)** до Ф1b-движка. Эмодзи (проекты/ордера) и
      list-глифы `purchaseLock` (●/▲) сняты. tsc/vite/oxlint зелёные.
      **Остаётся мелочью:** мета-строки detail-форм (`PurchaseView`/`ProcurementView` и 6
      ордеров) ещё показывают эмодзи 🔒/●/▲ — функции `purchaseLock`/`procurementLock`
      живы ради подписи-текста; глиф там свести к `StatusGlyph` следующим мелким проходом.
- [x] **Ось разбора на codicon `layers` (СДЕЛАНО 2026-07-22, находка Ивана).** Треугольники
      и кружки (`▲`/`●`/`✓`) оси покрытия/дефицита в проекте заменены на `layers`-семейство:
      `layers` красный (не заказано) → `layers-dot` оранжевый (заказано, ждём) →
      `layers-active` зелёный (на складе). Форма глифа одна («слои склада» — перекликается с
      режимом «Склады»), состояние несёт ЦВЕТ, не форма — не шумим треугольниками/кругами.
      Компонент `LayerSeg` в `status.tsx` (рядом со старым `Segment`); **пилот только в
      `DeficitView`** (проект + разбор строки, 3 триплета). Остальное (`CommandDeficitView`,
      `KittingView`, `PurchaseView`, `PeggingPanel`) пока на `Segment`/`Glyph` — раскатать,
      когда Иван решит «куда ещё пригодится».
- [x] **Ф1b-движок (СДЕЛАНО 2026-07-22):** `purchase_coverage(purchase)` → цвет Заказов
      (красный нет лотов / оранжевый часть / зелёный все; из `_line_received`);
      `project_health(project)` → цвет Проектов worst-of (красный есть `to_order` /
      оранжевый есть `on_order` ИЛИ прибор не собран / зелёный всё). Оба — новое поле
      `coverage`/`health` в `_purchase_row`/`_project_row`; фронт красит `StatusGlyph
      tone={statusTone(...)}`. **Канон кэша (оценка по запросу Ивана):** health НЕ храним
      в БД — это живая проекция как дефицит («не таблица»), денормализация протухла бы в
      списке. Считаем inline **лёгкой версией** `project_deficit(project, with_tree=False)`
      (без дерева-аккордеона). Внутренние/пустые проекты → `null` (нейтрально). Тесты:
      `PurchaseCoverageTests` (5) + `ProjectHealthTests` (5) — сюита 344 зелёная.
- [x] **Detail-мета шапок → codicon (СДЕЛАНО 2026-07-22):** эмодзи 🔒/●/▲ в мета-строках
      Заказа/Закупки/6 ордеров/PeggingPanel сведены к `StatusGlyph` (форма=фиксация, цвет=
      покрытие у Заказа, фиксация у прочих). Временные `purchaseLock`/`procurementLock`
      удалены. Список ↔ деталь консистентны.

**Риск (снят на данных):** удаление заказа упиралось бы в `PROTECT` от висящих
`Receipt` — в проде приёмок 0, живых данных под ударом нет. Friendly-guard'ы
(`delete_purchase`/`delete_procurement`) на месте и покрыты тестами.

---

## Ф2. Единый пикер везде — СДЕЛАНА 2026-07-25

Type-ahead из `AddComponent` (`ItemView`) признан эталонным; движок пикера вынесен и
раскатан на все места выбора. Охват **расширен по ходу** (решение Ивана): не только
изделие, но и контрагент с заказом — 11 снятых `<select>` вместо шести.

- [x] **Шов внутри пикера** (решение Ивана 2026-07-25 — вариант «логика сразу в
      `core/`»): `core/useTypeahead.ts` — чистая логика (кандидаты, подсветка,
      `↑`/`↓`/`Enter`/`Esc`), ни CSS, ни Codicon, ни компонентов вью; `Picker.tsx` —
      знак темы (`.lot-sel` + `.typeahead-menu` + `renderRow`). Каталог `core/`
      заведён этой фазой — первый житель раскладки Ф7.
- [x] Обёртки: `ItemPicker` (глиф `ItemGlyph` + `code` + описание), `CounterpartyPicker`
      (без глифа — у контрагента нет оси состояния; заведётся в волне 20),
      `PurchasePicker` (`StatusGlyph` + код заказа + строк). `AddComponent` стал тонкой
      обёрткой: `<select>`-копипаст и рукописный type-ahead удалены.
- [x] Изделие (6): `ProcurementView`, `PurchaseView`, `ReceiptView`, `InventoryView`,
      `DeficitView` (`＋ прибор`), `App.NewKitting` (цель комплектации).
- [x] Контрагент (4): `ProcurementView` (поставщик закупки), `TransferView` (заказчик),
      `App.NewReceipt` (поставщик УПД), `App.NewTransfer` (заказчик).
- [x] Заказ (1): `ReceiptView` — «Заказ (закрывает)».
- [x] **Автовыбор первого варианта снят везде** (решение Ивана): `DeficitView`,
      `App.NewKitting`, `App.NewReceipt` молча подставляли первый прибор/поставщика.
      Выбор всегда явный — цена ошибки (не тот прибор в акте, не тот поставщик в УПД)
      выше цены клика. Канон — `UI_GUIDE.md` §6a.
- [x] Правило «ввод не трогает выбранное»: на формах с немедленным сохранением набор
      текста не порождает PATCH и не блокирует поле через `busy`. Очистка — отдельным
      `×`, только у необязательных ссылок.
- [ ] **Не в этой волне:** пикеры лотов (`lot-sel` в Transfer/Writeoff/Requisition/
      Relocation/Kitting) — та же болезнь, но другой словарь строки (лот = изделие +
      партия + остаток). `renderRow` в `Picker` заложен ровно под это.

**ПРИНЯТА Иваном в браузере 2026-07-26** («так и хочется»). Доводка по итогам показа: (1) отступ от края меню приравнен к зазору
между колонками (12→8px) — глиф больше не отлипает от кода; (2) **ширина меню
ограничена** `min(680px, 100vw−64px)`: правая граница видна и не прижата к краю окна,
описание обрезается многоточием, `code` не жмётся; (3) **прокрутка за подсветкой** —
ход стрелками подтягивает список к активной строке. Прокрутку сделали в теме
(`Picker`), а не в ядре: движок говорит «какой активен», тема решает, как его показать.
Двигаем `scrollTop` самого меню по границам, а не `scrollIntoView` — тот уехал бы по
всем прокручиваемым предкам и дёрнул страницу.

**Выравнивать `code` в колонку внутри меню не стали** (решение Ивана 2026-07-26):
предложенный `min-width` у кода отклонён — «тут столбцов не нужно». Меню пикера —
сознательное отступление от §7a: там колонки нужны, потому что списки **сканируют**
глазом; меню же **читают** сверху вниз по одному кандидату, и воздух между коротким
кодом и описанием только разрывает строку. Не «чинить» задним числом.

Проверка: tsc/vite/oxlint зелёные, предупреждений 12 (было 14 — две ушли вместе со
снятым автовыбором в `DeficitView`). Бэкенд не тронут.

---

## Ф3. Изделие: три ортогональных бул + `code` — ОТДЕЛЬНЫЙ ЗАХОД

**Ф3a СДЕЛАНА 2026-07-24** (код + миграция `0007_item_axes_native_synced`; браузер за
Иваном). `produced → native`, `+ synced` (default False), `locked` — как есть;
`CheckConstraint(synced ⟹ not native)`; матрица `synced×locked` в `update_item`
(библиотечное расфикс → правится только цена); синк переработан (`refix → mark`,
`fixed → marked`): **новое** библиотечное рождается `synced=True, locked=False`
(готово под цену), **существующее** — синк метит `synced`, `locked` НЕ трогает (решение
Ивана). Глиф строки по режиму: Изделия → `StatusGlyph` (замок), Компоненты →
`SyncGlyph` (sync зелёный библ. / sync-ignored оранжевый ручной). Глифы навигации:
Проекты→`flag` (`\ec3f`), Компоненты→`chip`, Ордера→`notebook`. Сюита **350 OK**
(+инвариант, +матрица ×2), фронт tsc/vite/oxlint зелёные (oxlint 14 — прежнее).
Бэкфилл `synced` существующих библиотечных — первым же полным синком (диф-статус `mark`).

**Ф3a-добор СДЕЛАН 2026-07-25** (браузер за Иваном): (1) **2-й инвариант
`synced ⟹ not locked`** (`CheckConstraint`, миграция `0008`) — библиотечное защищено
матрицей «правь только цену», а не фиксацией; синк теперь всегда `synced=True,
locked=False` (снимает стухший замок волны 17). Грабля: `0008` упала на dev
(`IntegrityError 3819`) — после прогона синка 104 ряда стали `synced&locked`; добавлен
`RunPython` (снять `locked` у `synced` перед `AddConstraint`); тесты этого не ловили —
урок Ф1a про «план вслепую» повторился на инварианте. (2) **Глиф Item во ВСЕ списки**
(`ItemGlyph` в `status.tsx`, выбор по `native`): протащены `component_native/synced` в
leaf/tree/BOM, `item_*` в кокпит закупки, `target_*` для приборов-целей. (3) **Правило
вёрстки строк** (`UI_GUIDE.md` §7a): `[▸][глиф][code]`, `code` одной строкой с
многоточием, ровная колонка глифов; регрид дерева дефицита (`.prow` 7→6 колонок,
отступ по глубине `(depth+1)·шаг` — «всё отступает единым блоком», выбор Ивана).
Сюита **352 OK**, фронт зелёный.

**Ф3b СДЕЛАНА 2026-07-25** — см. ниже.

**Модель принята (2026-07-24).** «Компоненты» и «Изделия» — это один `Item` с **тремя
ортогональными бул**, каждый — самостоятельная ось. Разводит два смысла, которые сейчас
тащит один `locked`. Едет отдельной миграцией по Item (отдельно от Ф10), сюда же —
`design_item_id → code` (тот же файл/синк).

### Три оси

| Ось | `true` / `false` | Роль |
|---|---|---|
| **`native`** (замена `produced`; принято 2026-07-24) | наше, авторское / внешнее покупное | делит `Item` на режимы **«Изделия»** (`native`) / **«Компоненты»** (`not native`); изделия из списка компонентов **исключаются** (супер-совпадает с тем, что кнопка «Синхронизировать с библиотекой» есть только в Компонентах). Прилагательное-**свойство** (не действие, в отличие от `synced`/`locked`): «производимость» — врождённое качество, его не «совершают» |
| **`synced`** (замена `lib`; принято) | из библиотеки / ручной | у покупных: библиотечный → глиф `sync` **зелёный**, ручной → `sync-ignored` **жёлтый** (важно при сборке списка закупок). Ставит **синк**, руками **не снять**. У библиотечного правится **только цена**, у ручного — всё |
| **`locked`** (оставляем — отполирован везде) | зафиксирован / расфиксирован | та же механика, что у документов. У изделия — гейт **утверждённого BOM** (движок гейтит `add_bom_line`); у компонента — «ответственно, руки прочь» (отпугнуть случайный клик). Слабее, чем у документов, — не вкладываемся в UI locked-компонента |

### Инвариант (ортогональность не полная)

- **`synced ⟹ not native`** (библиотека = библиотека *компонентов*; наше производимое в
  неё не попадает). Закрепить `CheckConstraint(synced → not native)`; ось «библиотечный»
  у изделий **не показывать**.

### Матрица редактируемости (`synced` × `locked`)

| | расфиксирован | зафиксирован |
|---|---|---|
| **ручной** | правь всё | read-only |
| **библиотечный** | **правь только цену** | read-only |

Это и есть чистое решение старой цели Ф3 «править цену, не расфиксируя»: у библиотечного
**расфиксированного** цена жива сразу, без расфиксации-ресинка. `locked` — верхний,
более жёсткий слой. Одинокого живого поля среди мёртвых (трение Ф3-1) больше нет.

### Разрез Ф3a / Ф3b (по ripple, 2026-07-24)

Замер ссылок показал: **`design_item_id → code` = ~265 упоминаний** (бэк: engine 77,
tests 68, models 11, views 9…; фронт: api.ts 32, App 9, ItemView 9… — ~19 файлов). Это
**отдельная сессия целиком**. Поэтому Ф3 режется:

- **Ф3a** — три бул `native`/`synced`/`locked` + инвариант + матрица + правило глифов +
  удаление `Category.icon`. **Без** code-rename. `produced → native` ~48 ссылок — среднее,
  проходит за сессию. `synced` заливается `False`; библиотечные пометит **следующий синк**
  (он ставит `synced=True`) — без гадания по данным.
- **Ф3b** — `design_item_id → code` в одиночку, осторожно, своей сессией.

**Как легла Ф3b (2026-07-25).** Замер по факту дал **~308** упоминаний (больше оценки
265) в **шести** вариантах: `design_item_id` 202, `item_design_item_id` 60,
`component_design_item_id` 24, `target_design_item_id` 13, `item__design_item_id` 6
(ORM-lookup), `parent_design_item_id` 3. **Все шесть закрылись одной заменой по
подстроке** — префикс сохраняется сам (`item_` + `code` = `item_code`), кросс-ссылочные
ключи API стали `item_code`/`component_code`/`parent_code`/`target_code`. Замечания:
- **Миграции из замены исключены** (`0001_initial` обязан помнить старое имя, иначе
  `RenameField` не за что зацепиться). Perl, не BSD sed (грабля Ф10).
- **Коллизии ключа `code` в проекции нет:** `_item_row` отдаёт категорию **вложенным
  объектом** (`category: {code, description}`) — два кода живут на разных уровнях.
- **Снят дубль правила:** у Item была **своя** рукописная проверка занятости кода (в
  `create_item` и в `update_item`), при том что с Ф10 есть общая `require_unique_code`.
  Обе сведены на неё — минус одна реализация того же правила, текст отказа стал общим
  («Код «X» уже занят»). Тест дополнен проверкой сообщения.
- **CSV-контракт не тронут:** `LIBRARY_KEY_COL = 'Design Item Id'` и тексты ошибок
  парсера остались — это внешнее имя колонки Altium, переехала только цель маппинга.
- **Подписи форм** приведены к канону «Код»/«Описание» (ItemView и «＋ Новое изделие»
  звали поле «Изделие» — последний разъезд со [[form-consistency-standard]]).
- **Verify:** сюита **352 OK**; `tsc`/`vite build`/`oxlint` (14 warning — прежние);
  миграция накатана на **dev с реальными данными прода** (173 изделия) и **откачена
  обратно** — поле и `UNIQUE` возвращаются, строки целы.

### Работы

- [x] **(Ф3a)** Модель: `produced` → `native`; `+ synced` (default False); `locked` — как
      есть. `CheckConstraint(synced → not native)`. Миграция `0007_item_axes_native_synced`.
- [x] **(Ф3b)** `design_item_id → code` — **СДЕЛАНО 2026-07-25**, миграция
      `0009_item_code` (`RenameField`, `unique` переезжает с полем, `ordering=['code']`,
      verbose «код»). Итог: **исключений из единого интерфейса не осталось**.
- [x] Синк: ставит `synced=True`, руками не снимается. (CSV «Design Item Id» → `code` —
      контракт колонок за Ф3b.) `refix → mark`, `fixed → marked`; **новое** библ.
      рождается `synced=True, locked=False`; **существующее** — метим `synced`, `locked`
      не трогаем (решение Ивана 2026-07-24).
- [x] Движок `update_item`: матрица редактируемости (библиотечный расфикс → только цена).
- [x] **Глиф строки Item — РОВНО ОДИН, по режиму** (правило Ивана 2026-07-24): Изделие →
      `StatusGlyph` замок (ось `locked`); Компонент → `SyncGlyph` (`sync` зел. библ. /
      `sync-ignored` оранж. ручной). Друг друга не показывают.
- [x] Форма следует матрице (`metaLocked`); у компонента в мете «библиотечное/ручное».
- [x] README: обе диаграммы (`ITEM`: `native`/`synced`/`locked`) + текст. (`code` — Ф3b.)

**Глифы режимов навигации** (смена, решение Ивана 2026-07-24) — в `App.tsx` (`MODES`):
- [x] Проекты → **`flag`** (`\ec3f`, сверено с codicon.ttf)
- [x] Компоненты → **`chip`**
- [x] Ордера → **`notebook`**

**Прежняя формулировка Ф3 («цена вне гейта `posted`») устарела** — оси `posted`/`status`
нет с Ф1c, механика теперь через матрицу `synced`×`locked`.

**Имена осей приняты (2026-07-24):** `native` · `synced` · `locked`. `produced` уходит
(длинно/мутно). Обоснование выбора — в JOURNAL.

---

## Ф4. Контрагент у Закупки (функциональное поле для Ф6) — СДЕЛАНО 2026-07-24

> Идентичность Закупки/Заказа (`code`+`description`) переехала в **Ф10** (свёртка
> интерфейса). Здесь остаётся только **функциональное** поле, которого требует поток
> «Заказ → УПД».

**СДЕЛАНО (код, 2026-07-24; браузерная приёмка — за Иваном).** Сюита 346 (было 344,
+2 теста), tsc/vite/oxlint зелёные, новых warning нет. Миграция `0005_procurement_contractor`.

- [x] `Procurement`: `+ contractor` (FK `Counterparty`, `null=True`, `on_delete=SET_NULL`
      — удаление контрагента не роняет план-закупку; отличие от `Receipt.contractor`
      с `PROTECT` осознанное). Роль пикера — `is_supplier` (Р3: закупка = один поток
      общения с поставщиком). Питает Ф6 (`create_receipt_from_purchase`) и шапку xlsx-бланка
      (Ф4b). `update_procurement` — часовой `contractor=_UNSET` (как `update_transfer`);
      `procurement_cockpit` отдаёт `contractor_id`/`contractor_name`.
- [x] Форма `ProcurementView`: пикер поставщика (`api.counterparties('supplier')`) в шапке
      `.props` + контрагент в мета-строке (стандарт форм — [[form-consistency-standard]]).
- [x] README: техническая ER-диаграмма (`PROCUREMENT.contractor_id` + связь
      `COUNTERPARTY ||--o{ PROCUREMENT : "поставщик (nullable)"`). Продуктовая — flowchart,
      полей FK не несёт; глоссарий уже упоминает контрагента.

**Правки на приёмке (Иван 2026-07-24, «работает, нравится»):**
- [x] **Кнопка «Скачать» перенесена в шапку** (была меж шапкой и списком, в
      `.kit-actions` — «болталась»). Теперь: в слоте корзины (низ-право зоны шапки), но
      **только у ЗАФИКСИРОВАННОЙ** закупки (корзина там только у расфиксированной — слоты
      не сталкиваются). Глиф `file` (`ci-file` = `\ea7b`, добавлен), текст «Скачать»,
      **зелёная подсветка** (`--st-ok`, намёк на xlsx). Реализовано общим слотом
      `download?: {href,title}` в `FormHeader` (переиспользуемо).
- [x] **Имя файла = `Procurement.code`** — **СДЕЛАНО 2026-07-26**. `order.xlsx` как имя
      **упразднён целиком**: маршрут `procurements/<pk>/xlsx/`, вьюха `procurement_xlsx`,
      клиент `api.xlsxUrl`, файл скачивается как `<code>.xlsx` («Нева ДЗЗ 1.xlsx»), кнопка
      в шапке — просто «Скачать». Кириллический код требует RFC 5987 → заголовок через
      `content_disposition_header` (ASCII-фолбэк + `filename*=utf-8''…`), `_safe_filename`
      режет разделители путей, пустой `code` → `закупка-<id>.xlsx`. Тест
      `test_xlsx_filename_is_procurement_code`; сюита 353.

### Ф4b. xlsx-бланк — доработка (заказано Иваном 2026-07-20, делаем позже)

Первый файл ушёл живому контрагенту и **устроил**. Дальше — довести до вида бланка
запроса; это уже документ наружу, поэтому меняем осознанно и разом, а не по кусочкам.

- [ ] **Столбец «№»** — самый левый, порядковый номер позиции в запросе (не id изделия).
- [ ] **Шапка над таблицей:** заказчик · контрагент · дата запроса · автор запроса.
      Источники: контрагент — `Procurement.contractor` (Ф4, Р3); дата — `Procurement.date`;
      автор — `Procurement.user`; заказчик — наша сторона (уточнить: константа
      организации или поле).
- [ ] Порядок строк в файле = порядок позиций на экране (сейчас `order_by('id')`) —
      нумерация не должна разъезжаться с тем, что видит человек.

---

## Ф5. Форма закупки: один функциональный блок вместо двух

Сейчас закупка показана двумя разъехавшимися блоками: строки плана
(`procurement_cockpit`) и привязка к проектам (`procurement_pegging`). Пользователь
видит одно и то же изделие в двух местах и правит в разных. **Сливаем в один
раскрывающийся блок.**

> ⚠️ **Экспорт наружу не трогать.** С 2026-07-20 xlsx-бланк уходит **реальным
> контрагентам** (проверено на живом заказе). Файл собирается только из полей изделия —
> артикул / наименование / кол-во / ед. (`procurement_xlsx`, `engine.py`), собственных
> полей Закупки в нём нет. Поэтому переделка формы (Ф5) и новые поля имени/описания
> (Ф4) на глазах поставщика **не отражаются — и не должны**. Менять состав колонок
> бланка — отдельное осознанное решение, не побочный эффект правки формы.

### Целевая структура строки

```
▸ ✓ BGB707L7ESDE6327XTSA   усилитель    120 шт   ▸ разложено 100 / остаток 20
  └ ▸ РЕЗ-10К · Проект А        нужно 60   в заказ [ 60 ] → [Заказ #12 ▾]
  │   └ ИЗД-1 ×2 (потребность 20) = 40 шт     ← обратное разузлование
  │   └ ИЗД-7 ×1 (потребность 20) = 20 шт
  └ ▸ Проект Б                  нужно 40   в заказ [ 40 ] → [＋ новый заказ]
```

- [ ] **Движок: одна проекция вместо двух.** `procurement_cockpit` и
      `procurement_pegging` схлопнуть в `procurement_cockpit` (строки плана уже несут
      разбивку по проектам). Старую `procurement_pegging` и её маршрут удалить, не
      оставляя мёртвого эндпойнта.
- [ ] **Правка количества строки плана прямо здесь** (сейчас `qty` линии правится
      только в заказе; в форме закупки можно лишь увеличивать — шероховатость).
      `update_procurement_line` уже есть, довести до формы.
- [ ] **Обратное разузлование** — новая функция движка
      `item_usage_in_project(item, project)`: по потребностям проекта
      (`ProjectDemand`) обойти BOM целевых изделий и вернуть
      `[{target_item, qty_per_unit, demand_qty, total}]` — «по сколько штук этого Item
      идёт в то или иное производимое изделие проекта».
      **Переиспользовать существующую рекурсию** (`_explode_demand` / `_demand_tree`,
      `engine.py:494-590`), выделив общий обход, а не писать третий проход по BOM.
- [ ] **Пеггинг в конкретный заказ (Р2).** `peg_procurement_line` получает явный
      параметр `purchase=None`: передан — пегаем туда (проверив, что заказ под этим
      планом, этого проекта и в `draft`); не передан — прежнее поведение
      (найти-или-создать черновик проекта). `_project_purchase_under` остаётся
      фолбэком, а не единственным путём. В строке — выпадашка заказов проекта под
      планом + «＋ новый заказ».
- [ ] **Шероховатость из отчёта:** заказы не появлялись в списке «Заказы», пока не
      нажат «черновик заказа». Причина — заказ рождается лениво при первом пеге.
      Оставляем ленивое рождение (пустые заказы-призраки не нужны), но **веер заказов
      под планом виден прямо в форме закупки** — искать в другом режиме больше не надо.
- [ ] Аккордеон: раскрытие строки → проекты; раскрытие проекта → изделия-применения.
      Состояние раскрытия не персистим.

---

## Ф6. Заказ → УПД (прямая и обратная связь)

В 98% случаев накладная повторяет заказ 1:1 — набивать строки заново не должно быть
нужно. `Receipt.purchase` (FK, nullable) **уже существует** — достраиваем поток.

- [ ] **Движок: `create_receipt_from_purchase(purchase, user, number, date)`.**
      Создаёт `Receipt` с `project` из заказа, `contractor` — из
      `purchase.procurement.contractor` (Р3), `purchase` — ссылка на заказ; лоты
      преднабиваются по строкам заказа **остатком** (`qty − уже получено`, через
      существующий `_line_received`), `unit_cost` — из `item.estimated_cost` (или 0),
      имя лота — из `item.description`. Всё это черновик: пользователь правит перед
      проведением.
      Повторный вызов для частично закрытого заказа даёт вторую накладную на остаток —
      сценарий «поставка частями» работает без ручной работы.
- [ ] **Кнопка «Создать УПД» в форме заказа** → открывает готовый черновик прихода.
- [ ] **Обратная связь.** `purchase_cockpit`: по каждой строке — какими накладными она
      закрыта (`[{receipt_id, number, date, qty}]`). Источник — лоты прихода
      (`Lot.origin → Receipt.purchase`), сопоставление по `item`; отдельного FK
      «строка заказа ↔ лот» не заводим (схема не меняется, атрибуция по изделию
      однозначна — `PurchaseLine` уникален по `(purchase, item)`).
- [ ] В форме УПД — ссылка на заказ-источник (`ReceiptView.tsx:67` уже привязывает
      заказ вручную; теперь связь чаще будет проставлена сама).
- [ ] README: описать поток «Заказ → УПД» в разделе закупок; на диаграмме связь
      `PURCHASE ||--o{ RECEIPT` уже есть.

---

## Ф7. Шов «движок ↔ вью» + скелет второй темы

**Курс (формулировка Ивана):** «Я делаю движок, логику которого не дам никому менять.
Другие пользователи поверх этого движка делают свои вью». Сейчас фронт и бэк слегка
сросшиеся — это нормально для текущей стадии, но дальше расходимся строго.

### Что уже хорошо (не ломать)

Бэкенд почти готов к этому: `engine.py` — чистые функции, возвращающие словари-проекции;
`views.py` — тонкий HTTP-слой. CSS-токены (`index.css:49-75`) уже вынесены в `:root`.

### Найденные протечки представления в движок

- [x] ~~**`Category.icon` отдаёт коды Codicon** — тема протекла в API.~~ **Решено в Ф10
      УДАЛЕНИЕМ поля** (Иван 2026-07-24): per-категорийный глиф больше не нужен (различение
      разрешилось режимами Изделия/Компоненты). Протечка снимается сносом, а не
      семантическим переименованием — чище, чем планировалось.
- [ ] Ревизия остальных проекций на предмет UI-специфики (цвета, коды глифов, ширины,
      порядок колонок). Правило: **движок отдаёт смысл, тема выбирает знак.**
      Статусы (`to_order`/`on_order`/`available`) — смысл, остаются; `✓`/`▲` и цвета —
      тема.

### Раскладка фронта

```
frontend/src/
  core/            ← не зависит от темы: api-клиент, типы домена, хуки данных,
                     форматирование (money/num), словарь статусов (смысл, не глиф)
  themes/
    ide/           ← текущая тема (VS Code / JetBrains Mono), сюда переезжают
                     нынешние *View.tsx + index.css
    soft/          ← скелет второй темы: «надувные» кнопки, крупные отступы, «сайт»
  App.tsx          ← выбор темы + маршрутизация режимов
```

- [ ] Распилить `App.tsx` (1270 строк): загрузка данных и состояние → хуки в `core/`,
      разметка списков/оболочки → тема. Формы (`*View.tsx`) переезжают в `themes/ide/`
      **как есть** — переписывать их в этой волне не надо, надо только вынести из них
      обращения к API в хуки `core/`.
- [ ] Токены: разбить `index.css` на `core/reset.css` + `themes/<name>/tokens.css` +
      `themes/<name>/components.css`. Переключатель тем — атрибут на корне
      (`data-theme`), выбор в localStorage, дефолт — `ide`.
- [ ] **Скелет темы `soft`** (проверка, что шов держит): оболочка (навигация) +
      режим «Изделия» — список и форма. Самая отработанная часть продукта, значит
      самый честный тест. Остальные режимы в `soft` пока показывают заглушку
      «в этой теме ещё не оформлено» — это нормально и видно.
- [ ] **`docs/API.md`** — контракт как документ: перечень эндпойнтов, форма проекций,
      правила именования, что считается ломающим изменением. С этого момента
      изменение формы ответа — осознанный акт, а не побочка правки вью.

**Критерий приёмки шва:** ни один файл в `themes/` не ходит в `fetch`/`api` напрямую,
ни один файл в `core/` не импортирует CSS-классы и не знает про Codicon.

---

## Ф8. Гигиена артефактов (сквозная, каждую сессию)

Новая постоянная практика с этой волны: в каждой сессии **находить и выносить**
устаревшее — в принципах, схеме, движке, фронте. Не отдельной задачей, а фоном, с
записью найденного сюда.

**Реестр найденного (пополняется):**

| Артефакт | Где | Диагноз | Куда |
|---|---|---|---|
| `Purchase.Status.PARTIAL/RECEIVED` | `models.py:411-412` | мёртвые, 0 присвоений, 0 тестов | Ф1 |
| `Project.Status.DRAFT` | `models.py:311` | мёртвый | Ф1 |
| ✅ ~~`ITEM.status` отсутствует в диаграмме~~ | `README.md` | дрейф с волны 17 | **ЗАКРЫТ 2026-07-21** в Ф1c: в диаграмме `bool locked` |
| ✅ ~~`Category.icon` = коды Codicon~~ | `views.py:252` | тема протекла в API | **ЗАКРЫТ 2026-07-24** в Ф10: поле удаляется целиком (глиф разрешился режимами Изделия/Компоненты) |
| `_solo_procurement` | `engine.py:1104` | «1:1-заглушка», пережиток до волны 8 — проверить, жив ли путь | Ф5 |
| ✅ ~~Запись `0002_transfer_posted` в `django_migrations`~~ | прод-БД и dev | миграция-призрак: файл снесён при сквоше, запись (id=19, применена 2026-07-13) осталась. Найдена **первым же снимком прода** 2026-07-20 — на синтетике невидима в принципе | **ЗАКРЫТ 2026-07-20**: снят миграцией `0003_status_single_axis` локально и на проде (вывод деплоя: «призрак удалён (1 зап.)», миграция OK). Конвертаций статусов на проде не потребовалось — ни одной строки |
| `401` и `403` обработаны одинаково | `api.ts:395,411` | оба ведут `onUnauthorized` → на логин. Для истёкшей сессии верно, для CSRF-отказа маскирует настоящую ошибку (потерян день на «сломанный логин», JOURNAL 2026-07-20) | развести ветки: 403 показывать как ошибку, не разлогинивать |
| ✅ ~~Нет `CheckConstraint` на `status`~~ | `models.py` | `choices` — валидация уровня приложения, `.save()` её НЕ проверяет: `obj.status='banana'` проходил в `varchar(16)` | **ЗАКРЫТ 2026-07-21** даром в Ф1c: поля `status` больше нет, `tinyint(1)` мусор принять не может |
| Venv лежит по-разному локально и на проде | локально `backend/.venv`, на проде `<корень сайта>/venv` | грабля на ровном месте: команда, скопированная из локального контекста, на проде не находит python (поймано 2026-07-20) | либо выровнять раскладки, либо всегда писать прод-команды через `source venv/bin/activate` (рантбук поправлен) |
| Три словаря глагола замка в API (`post`/`approve`/`close`) | `urls.py` | одна ось — три имени маршрута; наследие волн 3/2/6 | **ЗАКРЫТ 2026-07-21** в Ф1c: везде `lock`/`unlock` |
| ✅ ~~`useFormLock(draft: boolean)` — параметр-обманка~~ | `FormHeader.tsx` | имя обещало «открыт, если черновик», но все вызовы передавали константу | **ЗАКРЫТ 2026-07-22** в Ф9: сигнатура `useFormLock(id, isNew)` — параметр честный (только что созданный ⇒ правка), + сброс режима на смену `id` |
| ✅ ~~Своя проверка занятости кода у Item~~ | `engine.create_item`/`update_item` | дубль правила: с Ф10 есть общая `require_unique_code`, а Item держал две рукописные копии с собственным текстом отказа | **ЗАКРЫТ 2026-07-25** в Ф3b: обе сведены на общую гвардию, текст отказа единый |
| Ручная фиксация изделия | JOURNAL 2026-07-17 | «осознанно временное» — модель фиксации ждёт консистентности | к пересмотру после Ф3 (**Ф3 закрыта 2026-07-25** — можно поднимать) |
| Нетинг при разузловании до листьев | WAVE16 Ф5 | помечен ⚠️ долг | не в этой волне, не терять |
| ✅ ~~Глиф `ci-chip` захардкожен в type-ahead BOM~~ | `ItemView.AddComponent` | строка кандидата рисовала «чип» всем подряд, мимо оси `native`/`synced` (устарело с Ф3a) | **ЗАКРЫТ 2026-07-25** в Ф2: строка пикера рисует `ItemGlyph` |
| ✅ ~~`Counterparty.code` не показан нигде~~ | все `<select>` контрагента | поле заведено в Ф10, а выбирали по-прежнему по `description` — код был мёртв в интерфейсе | **ЗАКРЫТ 2026-07-25** в Ф2: `CounterpartyPicker` показывает `code`, ищет по коду/описанию/ИНН |
| `AnchorSelect` / `ProjectField` / `AuthorField` — по-прежнему `<select>` | `FormHeader.tsx` | последние списочные `<select>` после Ф2: якорь-закупка у Заказа, проект и автор на всех ордерах. Пока списки короткие, но проект — растущий справочник | к Ф5/Ф7: перевести на `Picker` (нужен `renderRow` проекта и пользователя) |
| **Осиротевшая колонка `posted`** | `plume_transfer` в БД (dev И прод) | колонки нет ни в модели, ни в `0001_initial` — остаток саги о миграции-призраке `0002_transfer_posted` (JOURNAL 2026-07-20): запись из `django_migrations` вычистили, а колонку никто не удалял (Django не трогает то, чего не знает). Найдена замером `information_schema` 2026-07-26 | **Ф14**: умрёт вместе с таблицей бесплатно; без схлопывания — сносить руками |
| Протухший комментарий про `relocation` | `models.py:100` («новый вид, дочерней таблицы пока нет») | таблица `plume_relocation` и класс существуют | Ф14 (комментарий уходит вместе с MTI) |
| Пикеры лотов (`lot-sel`) | Transfer/Writeoff/Requisition/Relocation/Kitting | та же болезнь, что снята Ф2, но другой словарь строки (изделие + партия + остаток) | после Ф2 — `LotPicker` на заложенном `renderRow` (не в этой волне) |

---

## Ф9. Замок формы: режим правки, перетекающий между документами

**ЗАКРЫТА 2026-07-22** (код + приёмка Ивана в браузере). Все пункты сделаны, канон
`UI_GUIDE.md` §5 приведён в соответствие, журнал — запись 2026-07-22. Итоги — ниже.

**Замечено Иваном 2026-07-20.** Открыл форму на правку в одном документе, перешёл в
другой — тот тоже открыт на правку. Зафиксировал в одном — следующий тоже закрыт.
Режим ведёт себя как свойство **формы**, а не документа, и перетекает между записями.

**Это НЕ то же самое, что `locked` (Ф1c).** Здесь два разных замка, и путать их нельзя:

| | `locked` (Ф1c) | замок формы (эта фаза) |
|---|---|---|
| Что это | состояние документа | режим показа формы |
| Где живёт | в БД, поле | `useState` во фронте |
| Сервер знает | да, гейтит мутации | **нет, никогда не спрашивается** |
| Живёт после F5 | да | нет |

**Разбор кода (что нашлось сверх замеченного):**
- Причина протечки: `useOrderCockpit` (`FormHeader.tsx`) сбрасывает по смене `id`
  данные и ошибку (`setC(null); setErr(null)`), но **не трогает `unlocked`** —
  `useState` отдаёт начальное значение только при монтировании.
- **Дефолты разъехались между формами** (этого Иван не заметил, а это нарушение
  стандарта консистентности форм): ордера, Закупка и Заказ открываются **сразу в
  правке** (`useFormLock(true)`), а Изделие, Место и Дефицит — **в просмотре**
  (`useFormLock(false)`).
- **Сигнатура хука обещает то, чего нет:** `useFormLock(draft: boolean)` — имя
  параметра говорит «открыт, если документ черновик», но **все шесть вызовов передают
  константу**, а не состояние документа. Задумка была, реализации не случилось.

### Принят принцип (2026-07-20): **всё открывается в просмотре, кроме только что созданного**

Решение Ивана, и оно снимает и протечку, и разъехавшиеся дефолты разом. Формулировка
на одну фразу: **в справочнике Plume всё по умолчанию в просмотре, пока пользователь
явно не решил вносить изменения.**

Почему это правильно, а не вкусовщина:
- **Безопасность данных — главное.** Кнопки «Сохранить» в Plume нет: поля коммитятся в
  API по потере фокуса (`CommitInput`). Форма, открытая в правке, всегда в одном
  случайном нажатии от записи в БД, и отменить нечем. Открытие в просмотре делает
  случайную запись **структурно невозможной**. При живых пользователях это перевешивает
  эргономику.
- **Клик не добавляется, а убирается.** Сейчас ордера открываются в правке, а кнопка
  фиксации при открытом замке ЗАБЛОКИРОВАНА («сначала закройте замок — просмотрите
  чистовик») — то есть один принудительный клик есть всегда. По новому правилу форма и
  так в просмотре: «Зафиксировать» доступна сразу. Клик появляется только при реальной
  правке, по воле пользователя.
- **Ритуал «просмотрите чистовик» перестаёт быть ритуалом** и становится естественным
  состоянием формы.
- **Совпадает с сутью продукта:** в центре продуктовой диаграммы — линза, которая
  «следит» за всеми этапами. Plume сначала справочник и наблюдение, потом правка.
- **Уверенные правят осознанно, неуверенные спрашивают** и не ломают данные (Иван).

Отношение к `locked` (Ф1c): у запертой сущности просмотр **принудителен**, снятие —
через человеческий запрос («Расфиксировать»). У незапертой просмотр — **дефолт**,
правка в один клик. Правка ≠ «Зафиксировать»: первое обыденно, второе ответственно.

**Единственное исключение — только что созданный документ:** «＋ Новый заказ» открывает
пустую форму, смотреть там нечего — она открывается сразу в правке.

**Это не новое решение, а возврат к канону.** [UI_GUIDE.md §5](UI_GUIDE.md) с 2026-07-05
уже говорит: «Закрыт (по умолчанию у существующих объектов)… закрытый вид — основной вид
формы», и «у черновиков (только что созданных) замок открыт сразу». Канону следуют
Изделие, Место и Дефицит; ордера, Закупка и Заказ отъехали (`useFormLock(true)`).
Ф9 = приведение кода к собственному канону, а не изобретение правила.

- [x] Дефолт всех форм — просмотр (снимает разъехавшиеся `useFormLock(true/false)`):
      `useFormLock(id, isNew)` — существующее в просмотре, только что созданное в правке.
- [x] Сброс режима при смене `id` — эффект `[id]` в `useFormLock` возвращает режим к
      дефолту документа; протечка Ф8 (режим перетекал между документами) закрыта.
- [x] Только что созданная сущность открывается в правке: `App.justCreated` помечает
      id при создании и гаснет, как только выбор ушёл; `isNew` прокинут в каждую деталь.
- [x] **Контролы шапки — колонка справа** (`UI_GUIDE.md` §5): «подпись + Codicon»,
      иконки выровнены по правому краю. Codicons `edit`/`eye`/`lock`/`unlock`/`trash`
      (коды сверены с `codicon.ttf`). Индикаторы «✓ сохранено» / «● редактируется»
      сняты; ошибка — отдельной строкой `.fh-error` под шапкой.
- [x] **Глиф = назначение, не состояние:** «Зафиксировать» 🔒, «Расфиксировать» 🔓,
      «Редактировать» ✏️, «Просмотр» 👁. Карандаш на «Расфиксировать» не ставится.
      Последствие («…родить прибор», «отгружено») ушло в `title` кнопки — слово в
      кнопке едино «Зафиксировать» (Ф1c).
- [x] **Расфиксация оставляет форму в просмотре** (правка — отдельным решением).
- [x] **Корзина — свой постоянный нижний слот колонки** (не в слоте режима: в волне 16
      корзина занимала слот замка и сдвигала его — чинили симптом). **У зафиксированного
      документа контрол ровно один — «Расфиксировать»; корзины под замком нет** (движок
      не даст удалить запертое — мёртвая кнопка спорила бы со «состав = степень свободы»).
      Плавающая `.del-fab` (симптомное лечение волны 16) снесена.
- [x] Эндпойнтов здесь **не заведено**: режим формы серверу неизвестен.

**СДЕЛАНО (код, 2026-07-22):** `tsc` / `vite build` / `oxlint` зелёные (новых warning
нет). Переезд «Зафиксировать» из тела формы (`.kit-actions`) в колонку шапки затронул
все 12 detail-вьюх. **Браузерный клик — за Иваном.** Найденное сверх плана вычищено из
`UI_GUIDE.md` §4/§5 (устаревший чип-с-заливкой, индикатор сохранения, «фиксации у
изделия нет») — противоречий с реализованным не осталось.

#### Ф9 — приёмка Иваном (2026-07-22): три правки

Клик по dev: режимы 1–5 зелёные. Три доработки (объём — в конце):

- [x] **Корзина — к НИЖНЕЙ границе шапки, а не в плотную колонку сверху.** Разнесены:
      режим+фиксация — верх-право, корзина — низ-право (у нижней границы шапки).
      `FormHeader` принимает `children` (блок `.props`), оборачивает `[шапка]+[props]`
      в relative-зону `.fhz`; 🗑 — `.fh-del { position:absolute; right:0; bottom:0 }`.
      Обёрнуты все 12 detail-вьюх. Это то, что §5 требовал изначально — **был мой промах
      исполнения**.
- [x] **Корзина подписана «Удалить»** (как остальные контролы). §5-макет обновлён.
- [x] **Форма проекта в просмотре — контролы правки приборов спрятаны.** `AddDevice` и
      правка количества (`DeviceRow`) гейтятся на `unlocked && !closed`; в просмотре qty
      — plain-text, кнопки «+ прибор»/«×» скрыты.

- [x] **Корзина — только в режиме ПРАВКИ** (уточнение Ивана 2026-07-22, приёмка №2):
      условие `!fixed && unlocked && onDelete`. Просмотр чист, случайное удаление
      структурно невозможно, и заодно бесплатно снят казус «прилипшей» корзины у проекта
      в просмотре (нет свойств → нет низа шапки, но корзины там теперь и так нет).

**СДЕЛАНО (код, 2026-07-22, приёмка Ф9):** tsc/vite/oxlint зелёные, новых warning нет.
Браузерный клик — за Иваном.

**В бэклог (не сейчас):** кнопка «Удалить» у проекта — нужна ли в форме или только
через админку (пустой удаляется, непустой защищён — удобно). См. `BACKLOG.md` (Интерфейс).

---

## Ф10. Свёртка к единому интерфейсу (`code` + `description`) + удаление `note`

**СДЕЛАНА 2026-07-24** (код + миграция + проверки; браузерная приёмка — за Иваном).
Миграция `0006_code_description_interface`. Сюита **346 OK**; фронт tsc/vite/oxlint
зелёные (новых warning нет). Миграция накатана и **обратима на реальном снимке прода**
(4 проекта, 1 контрагент, 173 изделия): `name`/`label` сохранены в `description`,
новые `code` встали `NULL` без коллизий.

**Как легло (заметки исполнения):**
- **Выходные ключи проекций — дисциплина «идентичность против кросс-ссылки».** У
  сущности её СОБСТВЕННАЯ идентичность отдаётся как `code`+`description` (`_project_row`,
  `_location_row`, `_category_row`, `_counterparty_row`, кокпиты закупки/заказа/всех 6
  документов, пикеры). Денормализованные **кросс-ссылки** (`project_name`,
  `contractor_name`, `location_name`) оставлены как имена ключей, но питаются из
  переименованного поля (`.description`). Так пара идентичности чиста у каждой сущности,
  а API кросс-ссылок стабилен. Если захочется полной консистентности и по ним —
  отдельный мелкий проход (не обязателен для Ф10).
- **Документы: `code`/`description` — через ДЕТАЛЬНУЮ форму, не при создании.** Колонки
  живут на `StockDocument` (все 6 наследуют), проведены в проекции + `update_*` всех
  документов (общий помощник `_set_code` для часового `_UNSET`→пустой→NULL; `description`
  через `_apply`). `New*`-формы документов их НЕ собирают (шапка минимальна: проект +
  №). Только Procurement/Purchase (планирование) получают `code`+`description` в форме
  «＋ Новый».
- **Часовой `_UNSET` для `code`** (Procurement/Purchase/документы): пустой код → `NULL`
  (в MySQL несколько NULL не конфликтуют по unique). `description` — через None-skip.
- **Заголовки форм: `FormHeader name` = `code` (правка Ивана на приёмке 2026-07-24).**
  Изначально оставил вычисляемые заголовки; Иван на форме Item показал, что длинный
  `description` в H1 неудобен и «забирает опору». Итог: шапка = **`code`** (короткий
  уникальный жаргон), `description` — в `.props`. У сущностей с обязательным кодом
  (Item=`design_item_id`, Project/Location) — прямо; у документов/Procurement/Purchase
  (код nullable) — **мягкий фолбэк** `code || «УПД №»/«Заказ #id»/…`, чтобы шапка не
  пустовала и подталкивала заполнить код. `code`/`description` — редактируемые поля в
  `.props` ВСЕХ 12 форм (добавил в 6 форм документов, где их не было). Принцип
  зафиксирован в памяти для инструкции сотрудникам ([[code-identity-principle]]).
- **Мягкая уникальность `code`** (`engine.require_unique_code`): занятый код → дружелюбный
  отказ до `IntegrityError`/500 (Procurement/Purchase/StockDocument/Counterparty; пустой
  NULL легален). Тест `test_code_soft_uniqueness`. Сюита **347 OK**.
- **Оговорка про Item** закрыта позже: `design_item_id → code` сделан в **Ф3b**
  (2026-07-25), оси `produced→native`/`synced` — в Ф3a (2026-07-24).

Исполнение «Сквозного решения №2» (карта — там). Сквозной rename по почти всем моделям.
Делаем **разом и целиком** (прод отвязан). **Item держится в Ф3** (едет с `lib`/`locked`);
здесь — все **прочие** сущности.

### Модель + миграции

- [ ] **Project / Location**: `name` → `description` (rename, длина сохраняется).
      `code` уже есть. Verbose — «описание».
- [ ] **Category**: `label` → `description` (rename). `code` есть. **Удалить поле
      `string icon`** (решение Ивана 2026-07-24): коды Codicon категории **больше не
      нужны** — различение «что это» разрешилось в режимах Изделия/Компоненты, per-
      категорийный глиф отпал. Снять поле + все его использования во фронте (`ci ci-*` в
      строках/форме Item). Заодно **закрывает протечку Ф7** (`Category.icon` = коды темы в
      API) — удалением, а не семантическим переименованием. Проверить синк
      (`LIBRARY_CATEGORIES` — убрать иконки из данных категорий).
- [ ] **Counterparty**: `+ code` (уник.), `name` → `description`. Пикеры контрагентов
      (Receipt/Transfer) показывают `code — description`.
- [ ] **Procurement / Purchase**: `+ code` (уник.), `+ description`; **`note` удалить**.
- [ ] **Документы** (`StockDocument`, базовый класс): `+ code` (уник.), `+ description`;
      **`note` удалить**. `number`/`date`/`contractor` — не трогаем (юридические поля).
      `code` — наш ярлык («Нева ДЗЗ 1»), `number` — внешний номер накладной (заполняем
      по необходимости). Проверить friendly-guard'ы и генерацию `number`.
- [ ] **Стратегию заполнения `code` НЕ выбираем** (решение Ивана 2026-07-24): **человек
      вводит `code` сам**, авто-генерации/фолбэка **нет**. Посмотрим, как пойдёт — при
      нужде добавим позже. Следствие для схемы: у **новоприобретающих** поле сущностей
      (Counterparty, Procurement, Purchase, документы) `code` — **`null=True, unique=True`**
      (в MySQL несколько `NULL` не конфликтуют → существующие строки живут с пустым `code`,
      человек заполняет когда захочет; уникальность стережёт только непустые). У Item/
      Project/Location/Category `code` уже обязателен-уникален — там не трогаем.
- [ ] **Мини-формы `New*`** получают поле `code` (+ `description`) — человек вводит; пусто
      допустимо (`NULL`). Списки без кода показывают `description` (или `— #id` фолбэк-
      ярлык — забота вью, по шву Ф7).

### Движок + API

- [ ] Все проекции/сериализаторы: заменить `name`/`label`/`note` на `code`/`description`
      в выдаче. Затронуто (карта из анализа 2026-07-23): `_purchase_row`, `_procurement_row`,
      оба кокпита (`purchase_cockpit`/`procurement_cockpit`), `_item_row`/детали Item,
      строки документов, пикеры (counterparties, project_purchases).
- [ ] `create_*`/`update_*` для Procurement/Purchase/документов: принять `code`/
      `description`, убрать `note`. Сообщения-реликты `PROCUREMENT_LOCKED`/`PURCHASE_LOCKED`
      («…(unpost)») поправить на `lock/unlock` заодно (Ф8-находка).
- [x] Синк библиотеки: маппинг CSV «Design Item Id» → `code` — **сделан в Ф3b**
      (2026-07-25). Имя колонки в CSV (`LIBRARY_KEY_COL`) осознанно **не менялось**:
      это внешний контракт Altium, переехала только цель маппинга.

### Фронт

- [ ] Списки/сайдбар (`App.tsx`): везде показывать `code` (ярлык) + `description`.
      Точки: глобальный поиск-фид (`purchases.forEach` и др.), сайдбар закупок/заказов
      (сейчас `Закупка #${id}` / `name: p.note`), справочники.
- [ ] Формы (`*View.tsx`): шапка `.props` — пары «Код»/«Описание» единообразно на всех
      формах (стандарт консистентности — [[form-consistency-standard]]). `FormHeader name=`
      = `description`; мета-строка начинается с `code`.
- [ ] Удалить контролы `note` из всех форм.
- [ ] Типы `api.ts`: `name`/`label`/`note` → `code`/`description` в интерфейсах строк.

### README

- [ ] **Обе диаграммы** (продуктовая + техническая) — переименовать поля во всех
      затронутых сущностях; текст словаря (README «единый язык») — `code`/`description`.
- [ ] Глоссарий: явно назвать интерфейс «код + описание» как канон идентичности.

### Verify

- [x] Миграции данных проверить на **снимке прода** — сделано: `name`/`label` живы в
      `description`, `code` встал NULL без коллизий; миграция обратима.
- [x] Сюита зелёная (347); тест на мягкую уникальность `code` (`test_code_soft_uniqueness`).

---

## Ф11. Полировка идентичности и скелета формы (приёмка Ф10, 2026-07-24)

Появилась из приёмки Ф10. Иван: «боль уходит». Уточнения и мелкие правки UI, которые
достраивают `code` как **первичную идентичность, ведущую пользователя** ([[code-identity-principle]]).

**Принятые решения (2026-07-24):**
- **Обязательность `code` — НЕ через `NOT NULL`, а через UI-подталкивание.** `code`
  остаётся nullable; шапка формы и сайдбар показывают код (с фолбэком) так, чтобы
  пустой код бросался в глаза и хотелось его заполнить. Схему не трогаем.
- **Уникальность `code` — в пределах сущности** (мягкий guard уже есть). Глобальную по
  БД и обязательность — НЕ делаем, пока не заболело.
- **Форма = две части: ШАПКА + СПИСКИ.** У всех сущностей одинаковый UI: шапка (кнопки +
  свойства + здоровье) и списки (строки разного назначения). Курс на будущее; сейчас —
  правки ниже.

**Работы — ВСЕ СДЕЛАНЫ 2026-07-24** (одним заходом, «go, все пять»; браузер за Иваном).
Backend 347 OK, фронт tsc/vite/oxlint зелёные (oxlint 14 — прежнее число, новых нет):
- [x] **Сайдбар и ⌘K → `code`-forward.** `orderEntries`/`paletteEntries` + списки
      Закупок/Заказов → `e.code || <фолбэк>` (номер/#id). Заодно `+code` в 6 list-
      сериализаторах документов (`_receipt_row`/…/`_kitting_row`) + api.ts Row-типы (у
      документов кода в списке раньше не было).
- [x] **`FormHeader.name` → `FormHeader.code`** — проп переименован (определение + 12
      вызовов). Последний «name» в зоопарке снят.
- [x] **Кнопка «Назад» убрана** (`.crumb`-бар + мёртвый `.back` CSS). Popstate и
      ⌥←-шорткат (по `history`-стеку) не тронуты — браузерный «Назад» работает.
- [x] **Строки списков на всю ширину.** Сняты `table.grid { max-width:760px }` и
      `.pgrid { max-width:840px }`; `.cell-ellip` 340→600px (описание дышит в широкой
      строке). Шапку `.props` не трогал.
- [x] **Стиль «Удалить»/«Скачать» при наведении.** Рамка убрана (border остаётся
      прозрачным), фон → тонкий тон семантики (`color-mix` оранж/зелёный), текст =
      семантический цвет. Как «обычные» кнопки, но в цвете.

---

## Ф12. Канон формы: один лэйаут на все формы (принят 2026-07-26)

**Идея Ивана**, изложенная 2026-07-26 девятью пунктами и утверждённая после разбора.
Продолжение той же чистки, что закрыла зоопарк полей (Ф10/Ф3): теперь закрывается
**зоопарк форм**. Полный канон — [UI_GUIDE.md §13](UI_GUIDE.md); здесь — работы.

**Почему ПЕРЕД Ф5, а не после.** Ф5 переписывает форму закупки с нуля; канон после неё
означал бы верстать её дважды. Плюс Ф12 **оплачивает Ф7 авансом**: когда все 12 форм
ходят через один `FormShell`, вторая тема получает свой лэйаут и наследует всё
остальное — скелет темы из Ф7 дешевеет в разы. `FormShell` живёт в **теме**
(`themes/ide/`), не в `core/`: это знак, а не смысл ([[engine-view-seam]]).

### Находки разбора (2026-07-26), объясняющие болячки

1. **«Три кнопки растолкали название и поля».** Причина не в кнопках: `.props` лежит
   **не в шапке, а под ней** — `.fhz` = `[form-head: имя+мета │ колонка кнопок]` +
   отдельный блок `[.props]` (`FormHeader.tsx:176-220`). Зоны «поля» и «команды»
   вообще не в одной горизонтали, кнопки висят НАД полями. Ф12 строит честный
   двухколоночный флекс, а не подгоняет отступы.
2. **«Описание ютится в узкой строке».** Ширина полей захардкожена: `CommitInput
   width={240}` прописан руками в каждой форме. Плюс `.props dt { text-align: right }`
   при колонке `max-content` — длинная подпись («Температурный диапазон») раздвигает
   колонку меток и утаскивает все поля вправо.
3. **Вложения «во все формы» — это миграция, не фронт.** `Attachment` держит
   exclusive-arc ровно из ДВУХ владельцев (`item`, `document`) с
   `CheckConstraint attachment_exactly_one_owner` (`models.py:738-766`).

### Инвентарь табов (что получается по формам)

| Форма | Табы |
|---|---|
| **Изделие** | Состав · Применение · Склад · Движения · Файлы = **5** (уточнено на приёмке Ф12a) |
| **Проект** | Приборы · Потребность · Склад проекта (закрытие) · Файлы = **4** (+ панель бюджета) |
| **Закупка** (после Ф5) | Позиции плана · Веер заказов · Файлы = **3** |
| **Заказ** | Строки · Закрывающие приходы · Файлы = **3** |
| **6 ордеров** | Строки · Файлы = **2** |
| **Место** | На складе = **1** (файлов у Location нет — §13.8) |

**Приборы и Потребность на форме проекта — РАЗНЫЕ табы** (решение Ивана 2026-07-26):
в реальной работе ими пользуются в разных сценариях, а не вместе. Отрисовка
потребности — отдельный план Ивана, реализуем позже.

### Ф12a — скелет (пилот на двух формах) — СДЕЛАНА и ПРИНЯТА в браузере 2026-07-26

**Итог фазы:** канон формы (`FormShell`) обкатан на Изделии и Поставке и принят.
Приёмка шла в шесть заходов (доборы 1–6 ниже) — почти всё, что в них всплыло, оказалось
не «подвинуть пиксель», а правилами уровня продукта: они разложены по `UI_GUIDE.md`
§3 / §7a / §13. Развилки вида сняты: полоски под табами нет, радиус — один токен,
титул по центру, счётчики — в мете. Открытым остался один вопрос — **глиф строки склада**
(у партии нет своей оси состояния), он едет в Ф12c.

- [x] `FormShell` в теме (`frontend/src/FormShell.tsx`): титул (`code`, по центру
      панелей, пустой = пустой) + панель «Шапка» (поля │ команды) + слот меты (по
      центру) + табы + панель «Список». Пока лежит плоско в `src/` — слой темы
      целиком переедет в `themes/ide/` одним движением в Ф7.
- [x] CSS: общий класс `.panel` (поднят из `.budget`; `.budget` оставила себе только
      раскладку статов), радиус — единственный токен `--radius-panel: 6px`; табы =
      `.fh-ctl` + `.fs-tab` с глифом **слева**, активный подсвечен `--bg-sel`.
- [x] Шапка: колонка меток фиксирована (168px), поле — на всю оставшуюся ширину
      (`.fs-head .props dd input { width: 100% }`); пиксельные `width` снят­ы из шапок
      обоих пилотов (в строках таблиц остаются — там они уместны).
- [x] Мета: правило «не повторять поля» применено. Изделие: глиф оси + «используется/
      спящий» + подсказка матрицы правки (категория/ед.изм./температура/оценка ушли в
      поля). Поставка: глиф + «строк N · сумма X ₽» (номер/дата/проект — поля).
- [x] Табы: сброс на первый при смене `id`; индекс зажат в границы набора (у покупного
      изделия таба «Состав» нет — набор подвижен).
- [x] Мягкое предупреждение при уходе с формы с пустым/пробельным `code` (`alert` в
      cleanup эффекта по `id`; клик по корзине подавляет — удалённую не поминаем).
- [x] **Пилот: Изделие** (5 табов) **+ Поставка** (2 таба; выбрана Иваном вместо самой
      простой — тяжёлый ордер честнее проверяет канон).
- [x] `[РАЗВИЛКА]` полоска под табами — сделано **без неё**; Иван смотрит в браузере.
      Тогда же — радиус, центровка титула, счётчик в табе (см. ниже).

**Решения по ходу:**
- **Якорь-заказ Поставки переехал из тела в поля шапки** (был строкой `.kit-actions`
  под шапкой): это ссылка документа, а не список.
- Команды **не продублированы**: `FormCommands`/`FormCornerCommand` вынесены из
  `FormHeader` и общие у обоих лэйаутов — пока 10 форм ждут Ф12c, копий не будет.
- «Приход пуст.» → «Поставка пуста.» (глоссарий волны 14).

### Ф12a-добор — приёмка Ивана 2026-07-26 (12 пунктов, все сделаны)

Первый взгляд в браузере. Развилок не было — реализовано сразу, правила записаны
в канон (`UI_GUIDE.md` §7a/§13).

- [x] **Счётчики из табов → в мету.** В табах числа шумели, мета без них была скудной
      («непонятно, зачем она нужна»). §13.6.
- [x] **Табы Изделия:** порядок и подписи одним словом — Состав · Применение · Склад ·
      Отгружено · Файлы.
- [x] **Табы — в светлой полосе вверху панели-списка** (образец: окно агентов VS Code).
      Плавающие сами по себе, они путались с метой. Табы + тело = одна карточка. §13.7.
- [x] **Глифы табов синхронизированы с режимами навигации:** Состав→`chip`,
      Применение→`rocket`, Склад→`layers`, Отгружено→`notebook`, Файлы→`files`. §13.7.
- [x] **«Choose file» из таба «Файлы» → команда «Загрузить» в шапке** (`cloud-upload`,
      код сверен). В табе — чистый список. Потребовало вынести состояние в хук
      `useAttachments`; системный диалог открывается одноразовым скрытым `input`.
      **Это ополовинило Ф12b** — осталась только миграция владельцев. §13.8.
- [x] **Заголовок списка = первая строка** (тот же кегль, не мелкая подпись). §7a.
      Вариант «заголовок по центру колонки» — в BACKLOG (Иван сомневается).
- [x] **Ед. — всегда своя крайняя правая колонка** (давняя боль: «шт» плясали
      внутри ячейки кол-ва). Сделано в обоих пилотах; **на остальные формы едет в
      Ф12c**. §7a.
- [x] **Высоту шапки задают поля, не команды:** место под нижнюю команду добавляется
      только когда она есть — в просмотре поля больше не «зависают» в центре. §13.3.
- [x] **Значения полей выключены влево**, к своим подписям (выключка вправо приехала
      из числового `.qty-in`). §13.3.
- [x] «Температурный диапазон» → **«Температура»** (и в форме, и в «＋ Новое изделие»).
- [x] УПД: «Заказ (закрывает)» → **«Заказ»**; кнопка «открыть ›» снята — кликабельно
      само название заказа (§8: кликабельно то, что названо).
- [x] Команды шапки стали **списком** (`actions`): у Изделия их теперь две —
      «Переоценить» и «Загрузить».

### Ф12a-добор 2 — вторая приёмка 2026-07-26 (13 пунктов, все сделаны)

- [x] **Полоса табов — цвета тела панели**, активный таб = команда шапки под курсором
      (`--bg-hover`). Своего вида у таба нет. §13.7.
- [x] **Поле «Производимое» снято** с формы Item и с «＋ Новое изделие»: `native` — ось
      РЕЖИМА, а не свойство на правку (рождается тем, из какого режима нажали «＋»).
      Новое правило §13.4 «поля-оси режима в форме не показываем».
- [x] **«используется» из меты убрано**, осталось только «спящий». `used` шире вхождений
      (лоты, строки заказов, потребность, комплектации) — поэтому признак не выкинут
      целиком: он говорит «ссылок нет, можно удалять». Обратное молчит. §13.6.
- [x] **Мета = счёт по табам в их порядке, число впереди подписи, со склонением**
      («17 компонентов · 2 вхождения · 0 партий · 0 отгрузок · 0 файлов»). Хелпер
      `count()` в `status.tsx`. §3/§13.6.
- [x] **Глиф из меты формы убран** — состояние видно в списке и по доступным командам.
      Отменяет прежний пункт §13.6 «мета несёт глиф состояния».
- [x] **Мета Поставки — итог документа**: «3 позиции · 10 344 шт · 50 м · 40 000,00 ₽ ·
      0 файлов». Количество сворачивается **по единицам** (`qtyByUom`): штуки с метрами
      не складываем. §13.6.
- [x] **Проект в шапке ордера — кликабельный код** без расшифровки (`ProjectField`
      получил `onOpen`; `openProject` прокинут App → OrderForm → форма).
- [x] **Колонка меток — `max-content`** по самой длинной подписи этой шапки (фикс 168px
      оставлял гэп в Поставке). §13.3.
- [x] **Команды выровнены с полями:** колонка команд растянута на высоту шапки, строки
      команд той же высоты, что строки полей → «Просмотр» в одной строке с «Код»,
      «Удалить» — с последним полем. `position: absolute` у корзины снят. §13.3.
- [x] **Ширина полей — две ступени токенами темы** (`--field-w: 320px`,
      `--field-w-wide: 640px` у Описания) вместо «во всю оставшуюся ширину». §13.3.
- [x] Длинная подпись единиц → **«Единицы»** в шапке формы (в списках — короткая
      **«Ед.»**, §7a), «Оценка, ₽» → **«Оценка»** (рубль приезжает со
      значением в просмотре). §13.4.
- [x] **Деньги единым форматом** `1 200 300,00 ₽` — разряды пробелом, копейки всегда.
      `money()` больше не округляет до рубля; формат общий для бюджета, сумм
      документов и оценки изделия. §3.
- [x] Колонка единиц в списках подписана **«Ед.»**. §7a.

### Ф12a-добор 3 — «Отгружено» → «Движения» (2026-07-26)

Единственный пункт с работой на бэкенде — остальная приёмка была фронтовой.

- [x] **`engine.item_shipments` → `engine.item_movements`.** Узкая проекция (только
      строки передач) заменена лентой ВСЕХ ордеров, коснувшихся изделия: **рождения
      партий** (`Lot.origin` — поставка/комплектация/инвентаризация/требование, знак `+`)
      **и движения существующих партий** (`StockLine` — пайка, передача, списание,
      требование, перемещение; знак уже в `qty`). Свежие сверху; ордер без даты
      (черновик комплектации) уходит в конец.
- [x] **`StockMovement` намеренно НЕ читаем**: это проекция-остаток, её `source_type`/
      `source_id` пришлось бы резолвить обратно в документы. Лента строится из тех же
      двух мест, что и сама модель склада (`Lot.origin` + `StockLine`).
- [x] Проекция экрана: `shipments` → `movements` (`ItemShipment` → `ItemMovement`);
      таб «Отгружено» → **«Движения»**, мета — «N движений» вместо «N отгрузок».
- [x] **Ордер в ленте кликабелен** (§8): `openOrder(kind, id)` — общий вход по виду
      (вид приходит данными). Дубль-диспетчер в App свёрнут на него.
- [x] Словарь видов ордера (`ORDER_KINDS`/`ORDER_LABEL`) вынесен из `App.tsx` в
      **`orders.ts`** — знание домена, а не компонент (рядом с формой ломал fast-refresh).
- [x] Тест `test_item_shipments_projection` → `test_item_movements_projection`
      (рождение + передача, знак сохранён). Сюита **353 OK**.

### Ф12a-добор 4 — глифы и ровные строки (2026-07-26, третья приёмка)

- [x] **Колонка глифа `.gl`**: отступ от края = отступу глиф→код (был вдвое меньше —
      глиф «прилипал» к рамке). §7a.
- [x] **Все строки одной высоты**, включая заголовок (`line-height: 20px` у `th`/`td`)
      + верхний паддинг тела таба ужат: заголовок больше не выглядит «высокой первой
      строкой». §7a.
- [x] **Глиф в «Применении»** — `ItemGlyph` родителя (в проекцию `where_used` добавлены
      его оси `parent_native/synced/locked`); в «Движениях» уже был замок ордера.
- [x] **Глиф вложения**: ФОРМА = вид файла по расширению (`file-pdf`/`file-media`/
      `file-code`/`file-binary`/`file-zip`/`file`), ЦВЕТ = живость на диске —
      `engine.attachment_state`: `ok` зелёный (совпадает с записью), `changed`
      оранжевый (размер/mtime разошлись — перезаписан мимо Plume), `missing` красный
      (записи есть, файла нет). Допуск по времени — 1 минута (диск штампует mtime
      чуть позже, чем БД `uploaded_at`). Тест `test_state_tracks_file_on_disk`.
- [x] **Колонки таба «Файлы»**: глиф · Файл (кликабельно = скачать) · Подпись · Размер ·
      Загружено · Загрузил · «Скачать» (`download`, оформлена как команда шапки) + `×`.
      Дубль «имя-ссылка + кнопка» оставлен намеренно — Иван смотрит глазами.
- [x] Глиф команды «Загрузить» — **`new-file`** вместо `cloud-upload`.
- [ ] **Глиф строки склада (партии) — придумать.** У партии своей оси состояния пока
      нет (замок? живой остаток? origin?). Решаем отдельно, вместе с Ф12c.

### Ф12a-добор 5 — вложения и колонки (2026-07-26, четвёртая приёмка)

- [x] **`Attachment.label` → `description`** (миграция `0010_attachment_description`,
      RenameField — данные едут с полем). Последний `label`-как-подпись в схеме: Ф10
      свела все сущности к паре «идентичность + описание», вложение оставалось в
      стороне. Идентичность файла — `filename` (своего `code` нет), поэтому описание
      идёт **сразу за ним** — и в модели, и в диаграмме README, и в списке.
      Накат и **откат** проверены на dev с данными прода.
- [x] Выключка описания вложения — влево, как у всех описаний (пиксельный `width` снят).
- [x] **«Загрузил» — человеческое имя**, не логин (`get_full_name() or get_username()`,
      тот же выбор, что у авторства документов `_author`).
- [x] Команды строки — **одними глифами**: `download` (скачать) и `trash` (удалить,
      в стиле «Удалить» шапки). Подписи ушли в `title`. §7a.
- [x] **Подчёркивание у команд убрано везде** (`.fh-ctl { text-decoration: none }`) —
      команда не ссылка-текст. §7a.
- [x] **Клик по имени ≠ «Скачать»** — проверено глазами: имя ОТКРЫВАЕТ файл во вкладке,
      кнопка СКАЧИВАЕТ. Дубль оправдался, оба оставлены. §13.8.
- [x] **Замещающий текст пустого таба** (`.tab-empty`) дышит одинаково сверху и снизу —
      не липнет к полосе табов.
- [x] **Колонка идентичности — общей ширины во всех табах** (`--col-key`):
      «Описание» больше не переезжает при переключении Состав ↔ Применение. §7a.
      Правый край («Кол-во», «Ед.») так работал и раньше — теперь и левый.

### Ф12a-добор 6 — распределение ширины и корзина в строке (2026-07-26)

- [x] `--col-key` **260px → 160px** (−3/8): коды короткие, лишнее место отдано описанию.
- [x] **Ширину забирает описание** (`.c-desc` — весь свободный остаток), служебные
      колонки (размер / дата / автор) жмутся по содержимому (`.c-fit`) и уезжают вправо,
      к командам строки. Ушло мёртвое поле между обрывом описания и числами. §7a.
- [x] **Корзина в строке была не видна — починено.** Причина: `.fh-del` в шапке
      позиционируется `absolute` (нижний слот панели), и в строке списка кнопка
      уносилась из потока. В таблице возвращена в поток (`position: static`).
- [x] **Удаление вложения — только в режиме ПРАВКИ** (`AttachmentList` получила
      `locked`): то же правило, что у корзины шапки (§5). «Скачать» доступна всегда,
      описание под замком не правится.
- [x] **Крестик удаления → кнопка-корзина** (принято Иваном на месте: «слишком
      хорошо»). Раскатано по формам, которые правились в этой фазе: вложения, состав
      изделия, строки поставки. Правило — §7a; остальное едет общим проходом Ф12c.
- [x] **Поле описания тянется вместе со своей колонкой.** У `CommitInput` был
      **дефолт `width = 60`**, а inline-стиль сильнее любого класса — колонка
      расширилась, контрол остался узким. Дефолт снят: ширина задаётся только там, где
      её действительно выбирают (ячейки кол-ва/цены), иначе решает CSS. Ровно та же
      болезнь, что «ютящееся описание» в шапке (Ф12a, §13.3) — второй её очаг.

---

## Ф12d. Чистка вёрстки шапок (заявлена 2026-07-26, НЕ начата)

**Заявка Ивана:** «У нас всё ещё какие-то проблемы с вёрсткой шапок. Запиши чистку
вёрстки шапок как отдельную фазу — чтобы мы детально разобрали это, нашли причины, по
которым элементы никак не укладываются в ровный грид, и вычистили их».

Фаза **диагностическая**: не «подвинуть на 2px», а найти и снять источники кривизны.
Известные подозреваемые (собраны по ходу Ф12a — начинать с них):

- **Три системы координат в одной панели.** `.props` — CSS-grid (строки по контенту),
  колонка команд — flex-column (строки по кнопкам), значения полей — то `<input>`
  (высота от рамки+паддинга), то голый текст (высота от line-height). Ровный грид
  из этого не получается по построению — нужен ОДИН источник высоты строки.
- **Просмотр и правка дают разную высоту строки**: инпут ≈ 22px против текста ≈ 18px,
  поэтому «в одну строку» сходится только первая пара, дальше расхождение копится.
- **Чекбоксы и пикеры — свои высоты** (`.picker` — inline-обёртка, `<select>` — своя
  метрика ОС), сетку они не держат.
- **`dt` и `dd` не связаны по базовой линии** — выключка подписи вправо маскировала
  проблему, но не решала.
- **Разнобой источников ширины:** токены темы (`--field-w`), `max-width` у `.lot-sel`,
  остатки пиксельных `width` в не переехавших формах.

**Как делать:** сначала измерить (по одной форме в просмотре и в правке), выписать
фактические высоты, затем выбрать единицу строки шапки и подчинить ей ВСЕ элементы
(включая инпуты, селекты, чекбоксы, пикеры и кнопки). Идёт **после Ф12c**, когда все
формы уже на `FormShell` — иначе чинить придётся дважды.

### Ф12b — вложения на все формы — СДЕЛАНА 2026-07-26 (backend)

**Фронтовая половина уехала в Ф12a-добор** (команда «Загрузить» в шапке, таб «Файлы»
= чистый список, хук `useAttachments`) — здесь остался backend.

- [x] Миграция **`0011_attachment_owners`** (не `0010` — тот занят `attachment_
      description` из Ф12a-добора 5): новые FK владельцев `Attachment` — **project,
      procurement, purchase, counterparty** (+ существующие `item`, `document`), все
      `CASCADE`; `ATTACHMENT_OWNER_FIELDS` 2 → 6, `_exactly_one_q` генерик — CHECK
      пересобрался сам. **Location и Category — НЕ добавляем** (решение Ивана: когда
      заболит). Данных миграция не касается (старые вложения уже лежат ровно на одном
      владельце) → симметрично обратима; накат + **откат** + повторный накат проверены
      на dev со снимком прода (3 вложения целы).
- [x] API: `views.attachments` править **не понадобилось** — диспетчер и так идёт через
      `engine.resolve_attachment_owner`; расширены `ATTACHMENT_OWNER_MODELS` и карта
      `owner_type → поле` (тернарник `'item' if … else 'document'` стал словарём:
      имя типа = имя поля у всех, кроме шести видов ордера, схлопнутых в `document`).
- [x] Команда **«Загрузить»** (`cloud-upload`, код сверен фонттулзом) в колонке шапки.
- [x] Таб «Файлы» = список вложений (`AttachmentList`), поле выбора файла снято.
- [x] Контрагенту вложения нужны под **«карточку предприятия»** (docx/pdf с
      реквизитами): сейчас файл болтается по чатам. Форма контрагента появляется
      в **волне 20** — владелец заведён в модели и ждёт свою форму.
- [x] **Снос физических файлов при удалении владельца** — тот же цикл
      `delete_attachment`, что стоял у ордера и изделия, добавлен в `delete_project` /
      `delete_procurement` / `delete_purchase`: каскад БД унёс бы строку, оставив файл
      сиротой на диске. **У контрагента пути удаления нет вообще** (нет эндпойнта, форма
      — волна 20) → сноса файлов там пока не существует; **завести вместе с формой
      контрагента** (долг волны 20).
- [x] Две проверки утверждали обратное и переписаны: `test_unknown_owner_type_rejected`
      («purchase НЕ владелец» → теперь `location`) и HTTP `test_bad_owner_type`. Обе
      **продолжали бы проходить** после правки — по другой причине (тип известен, записи
      нет), то есть молча стали бы бессмысленными. Новое покрытие: своё поле у каждого
      не-ордерного владельца, снос файлов при удалении владельца, «тип известен, записи
      нет» → 400. Сюита **357 OK**, фронт зелёный (oxlint 13 — прежние).
- [x] БД-инвариант перепроверен вживую: сырой INSERT с двумя владельцами и с нулём
      владельцев отбит MySQL (`3819 Check constraint attachment_exactly_one_owner`) —
      дуга держится на шести полях, а не только в `clean()`.

### Ф12c — раскатка на все формы

- [ ] Остальные 10 detail-вьюх + 4 панели (Pegging/Closure/ProjectStock/Attachment)
      под `FormShell`.
- [ ] **Ед. — своя крайняя правая колонка во ВСЕХ списках** (§7a, принято на
      приёмке Ф12a; в пилотах сделано). Сквозная чистка: «шт» больше нигде не приклеена
      к числу внутри ячейки кол-ва. Затрагивает дерево дефицита/потребности, формы
      6 ордеров, Заказ, Закупку, панели закрытия и остатков.
- [ ] Старая `AttachmentPanel` (заголовок секции + кнопка над списком) исчезает вместе
      с последней не переехавшей формой — её роль играют команда шапки + таб.
- [ ] **Крестики-`×` в строках → кнопка-корзина** (§7a). В отредактированных формах
      сделано (вложения, состав изделия, строки поставки); остались 5 ордеров, Заказ,
      Закупка, дерево дефицита, `PeggingPanel`. Крестик `×` у пикера НЕ трогать —
      это очистка поля, другое действие (§6a).
- [ ] **Колонка идентичности `.c-key` — во все списки форм** (сделано в табах Изделия
      и в «Файлах»): у остальных форм колонки всё ещё разъезжаются между табами.
- [ ] **9 форм «＋ Новый» — ВСЁ ПОД КАНОН** (решение Ивана 2026-07-26). Сейчас это
      **второй, параллельный скелет формы** (`<h1 class="title">` + `.subtitle` +
      `.props` + `.kit-actions` с «Создать»), `FormHeader` в них не участвует вообще.
      Формулировка Ивана: «по кнопке ＋ Новый **планируется новая сущность**; если она не
      будет сразу удалена — попадёт в БД и начнёт жить своей каноничной жизнью; если
      пользователь ничего не заполнит — исчезнет». То есть отдельный скелет не
      «приводится к канону», а **исчезает**: клик рождает сущность, и открывается
      обычная детальная форма (Ф9 уже открывает только что созданное в правке).
      **Механика — доуточнить при реализации** (три вопроса, ответы нужны до кода):
      (1) момент INSERT — на клик или на первый заполненный символ; (2) чем «исчезнет» —
      авто-удаление пустой при уходе с формы или уборка периодикой; (3) **обязательные
      якоря**: `StockDocument.project` NOT NULL, у 6 видов ещё и `date`+`number`
      обязательны на фиксации (`REQUIRED_HEADER_BY_KIND`) — пустой ордер родиться не
      может без ослабления якоря или мини-преформы «проект + вид». У
      Item/Project/Location/Procurement/Purchase такой преграды нет.
- [ ] Попутно закрываются накопленные долги: антипринцип §7a в `PurchaseView`
      (`code`+`description` слитой ячейкой), `.subtitle`-описания (§12 их запрещает),
      разнобой `.kit-actions` (14 мест), `AnchorSelect`/`ProjectField`/`AuthorField`
      → `Picker` (реестр Ф8).

**Оценка объёма:** ~2.5 сессии (a ≈ 1, b ≈ 0.5, c ≈ 1). Волна удлиняется осознанно:
после Ф12 формы Ф5/Ф6 пишутся сразу набело, а Ф7 становится почти формальностью.

---

## Ф13. Охват закупки: свод становится обычной Закупкой (идея Ивана 2026-07-26)

**Идея.** У Закупки появляется **набор проектов**, под которые она ведётся («галочки на
проектах»). Итоговое количество собирается **только из выбранных**. Отметить все =
сегодняшний «Командный свод», но в виде канонической формы конкретной сущности.
Отметить один = прозрачная закупка под один проект, без шума остальных.

### Почему ложится в ценности (оценка 2026-07-26)

1. **Модель догоняет собственный глоссарий.** CLAUDE.md уже говорит: «Закупка =
   планирование… **может охватывать несколько проектов**». В модели этот охват сегодня
   **не выражен** — он подразумевается через *отсутствие* проекта у `Procurement`
   («командная высота, без проекта»).
2. **Снимает последний «общий через NULL».** Прецедент уже решён по складу (README:
   «общего через `NULL` нет — его заменяют внутренние проекты»). Закупка без проекта —
   ровно тот же антипаттерн, доживший до сегодня. Идея его закрывает.
3. **Убивает кастомный экран** (`UI_GUIDE` §13.10). «Командный свод» — фантом: пункт
   сайдбара без сущности, без `code`, без фиксации, без вложений, вечно один, со
   **своим третьим скелетом формы** (`CommandDeficitView`: `h1.title` + `.subtitle` +
   таблица). Ф12 его не покрывает — канонизировать нечего. Как таб обычной закупки он
   канонизируется даром.
4. **Дефицит остаётся проекцией, не таблицей.** M2M задаёт **область** расчёта, а не
   хранит числа — канон линзы («дефицит — не таблица») не нарушен.
5. **Чинит шум в Ф5 бесплатно.** Наводка пеггинга берётся из `command_deficit()`
   (`engine.py:2576,2692`) — то есть в аккордеон ЛЮБОГО плана сейчас подсовываются
   проекты всей организации. С охватом наводка сузится до выбранных — та же правка
   лечит будущую форму Ф5.

**Риск «забытая галочка» — ОТКЛОНЁН Иваном 2026-07-26 как выдуманный.** Аргумент:
«закупка — настолько важный и редкий процесс, что "забыть закупиться на проект" звучит
как "мы вообще закупкой не занимались"». Обратная связь и так придёт по нормальному
пути: закупка разрешается в заказы, заказы фиксируются — и в своде проекта компоненты
становятся оранжевыми. Это работа и внимание человека, а не автоматики.
**Предупреждения о проектах вне охвата НЕ делаем**; «разведку вбок» (подглядывание в
соседние проекты) не заводим. Ответственность остаётся на человеке.

### Работы

- [ ] **Модель:** `Procurement.projects` — M2M на `Project` (through-таблица авто;
      миграция `0012`). `Purchase.project` (единственный, проектное исполнение) **не
      трогаем** — это другой уровень.
- [ ] **Движок:** `command_deficit()` параметризуется набором проектов и становится
      **одной** функцией на оба сценария; проекция охвата отдаётся в
      `procurement_cockpit` (таб «К закупке»). Наводка пеггинга (`peg`/`autopeg`)
      считается по охвату закупки, а не по всем проектам.
- [ ] **Снос:** эндпойнты `/command-deficit/` и `/command-deficit/add-to-procurement/`
      (`urls.py:108-109`), `command_deficit_add` (магия «найти-или-создать
      расфиксированную закупку» — она перестаёт быть нужна: позиция кладётся **в эту**
      закупку), `CommandDeficitView.tsx` (115 строк) и пункт `extraTop` в сайдбаре.
- [ ] **Контрол выбора проектов** — `[ДОПОЛИРОВАТЬ ТЕХНИЧЕСКИ]`. Ориентир Ивана —
      «выбор папки для чата в Telegram»: раскрывающийся список проектов с галочкой на
      строке. Требование: **форма растёт не более чем на одно поле**
      (`Проекты  [ДО, Нева ДЗЗ +2 ▾]`). Опора есть: `core/useTypeahead` + `Picker`
      (Ф2) с готовым `renderRow` — нужен режим множественного выбора (Enter
      переключает галочку, а не закрывает меню). Отдельный контрол не изобретаем.
- [ ] **README:** обе диаграммы (`PROCUREMENT }o--o{ PROJECT`) + текст глоссария про
      охват закупки.

### Порядок: Ф13 исполняется ВМЕСТЕ с Ф5

Обе фазы переписывают одну и ту же проекцию (`procurement_cockpit` + наводка пеггинга).
Разнести их = переписать её дважды. Разрез: **Ф5a** = охват проектов + единая проекция
(движок и модель), **Ф5b** = аккордеон и пеггинг поверх уже суженных данных.

---

## Ф14. Снос MTI: семь таблиц ордера → одна (анализ 2026-07-26)

**Идея Ивана:** убрать MTI из `StockDocument` так же, как «Изделия и Компоненты»
разрешились одной таблицей `Item`. Анализ подтвердил выигрыш; ниже — факты, поправка
и раскладка.

### Замер: что реально различается у семи видов

Общее в родителе: `kind · locked · project · user · code · description · date · number`
(8 полей). **Специфика детей — 6 колонок на все семь видов:**

| Вид | Свои поля | Таблица-ребёнок |
|---|---|---|
| **Receipt** | `contractor` (PROTECT, NOT NULL), `purchase` (SET_NULL) | 2 колонки |
| **Kitting** | `target_item` (PROTECT), `qty` | 2 колонки |
| **Transfer** | `contractor` (PROTECT, **nullable**) | 1 колонка |
| **Writeoff** | `reason` | 1 колонка |
| **Inventory / Requisition / Relocation** | — | **пустые** (только `stockdocument_ptr_id`) |

Проверено запросом к `information_schema` на dev-БД со снимком прода: три таблицы
содержат ровно одну колонку — свой PK. Семь таблиц ради шести колонок, из которых
`contractor` встречается дважды с разным смыслом (поставщик / заказчик).

### Целевая схема

Одна таблица + существующий дискриминатор `kind` + 6 nullable-колонок + CHECK по виду:

```
kind='receipt'  → contractor NOT NULL
kind='kitting'  → target_item NOT NULL AND qty NOT NULL
kind NOT IN ('receipt','transfer') → contractor IS NULL
kind <> 'receipt' → purchase IS NULL
kind <> 'kitting' → target_item IS NULL AND qty IS NULL
```

MySQL 8.0.25 CHECK **применяет** — доказано на своей же грабле: `item_synced_implies_
not_locked` живёт в БД, на нём словили `IntegrityError 3819` (Ф3a-добор).

**Ключ к дешевизне — дети становятся `proxy`-моделями** с kind-фильтрующим менеджером.
Тогда `models.Receipt.objects.filter(...)` и все **62** per-kind функции движка
работают без правки: исчезают таблицы, а не API кода.

### Поправка к предложению: таблицу видов документов НЕ заводим

Иван предлагал «таблицу категорий документов». Аналогия с `Item` здесь ломается:

- **`Item.category` — таблица, потому что категории это ДАННЫЕ:** приезжают из
  библиотеки Altium, растут, правятся человеком.
- **Виды документов — это КОД.** Каждый вид = зашитое поведение движка (приход рождает
  лоты, комплектация списывает и рождает, перемещение двигает парой знаковых строк).
  Новый вид нельзя завести из интерфейса. Таблица врала бы, обещая обратное.
- **CHECK требует литерала:** `kind='receipt' → …` самодокументирован и стабилен;
  с FK пришлось бы писать `kind_id=3` — привязка к id строки, ломается при
  пересидировании, нечитаема.

Правильная аналогия с `Item` — не `Category`, а **`native`**: одна таблица +
дискриминатор-код. `kind` у нас уже ровно такой и уже есть. Заводить нечего — надо
**убирать таблицы**.

### Оценка по четырём осям (запрос Ивана)

- **Каноничность — высокая.** Учебная single-table inheritance с дискриминатором и
  CHECK. Главный аргумент не в этом: **интерфейс уже считает их одной сущностью** —
  режим «Ордера» один, `FIELD_MATRIX` B = единый row-set, `OrderForm.tsx` = один вход
  `<OrderForm kind=…>`. Бэкенд остался последним местом, где их семь.
- **Чистота — высокая, с честной оговоркой.** Уходят: 7 таблиц → 1, три пустые
  таблицы, JOIN на каждом чтении, **7 мест downcast'а** (`line.document.kitting`,
  `lot.origin.receipt` — каждый лишний запрос), 6 `update_*` → 1. **НЕ уйдут** 62
  per-kind функции движка: их различие поведенческое, а не структурное. Обещать
  «сократим движок вдвое» было бы неправдой.
- **Удобство — среднее сейчас, высокое потом.** Новый вид перестаёт стоить
  таблицу+миграцию: значение enum + поведение.
- **Скорость — выигрыш есть, но не он мотив.** MTI = JOIN на чтение, 2 INSERT на
  запись, запрос на каждый downcast. При наших объёмах это шум.

### Цена и окно

**Перевязывать ссылки не придётся:** `Lot.origin`, `StockLine.document`,
`Attachment.document` **уже указывают на родителя** — волна 13 (Ф2b) сделала тяжёлую
часть заранее. Миграция: добавить 6 колонок → скопировать из детей → удалить 7 таблиц.
Симметрично обратима.

**Объём данных (замер dev-БД со снимком прода, 2026-07-26):** `stockdocument 1 ·
receipt 1 · остальные шесть видов 0 · lot 1 · stockline 0 · movement 1`. Один документ
на всю базу — дешевле эта миграция не будет никогда. **Перед прогоном пересняться**
(`pull_prod.sh`): снимок может быть несвежим, пользователи тестируют с 13.07.

### Работы

- [ ] Модель: 6 nullable-колонок в `StockDocument` + 5 `CheckConstraint` по `kind`;
      семь детей → `proxy` с kind-фильтрующим менеджером.
- [ ] Миграция `0013`: перенос данных из детей → снос семи таблиц (обратима).
- [ ] Движок: 7 downcast-сайтов (`views.py:545,699,985,1236,1340,1469`,
      `engine.py:1402`) — прямое обращение к полю; 6 `update_*` свести в один.
- [ ] Админка: шесть `ModelAdmin` переезжают на proxy (регистрация не меняется).
- [ ] README: обе диаграммы (MTI-стрелки → одна сущность с `kind` + специфика).
- [ ] Verify: сюита 352 должна остаться зелёной **без правок тестов** — если правки
      понадобились, значит proxy-слой протёк.

### Порядок

**После Ф5/Ф6, до выката хвоста миграций.** С Ф12 не пересекается (фронтовые тела
шести видов несводимы в любом случае — так и написано в `OrderForm.tsx`), поэтому
порядок свободен; но прод выкатывать дважды не хочется — схема должна устояться до
одного согласованного прогона.

---

## Порядок исполнения

Зависимости диктуют такой порядок:

1. **Ф1** (статусы → `locked`) — ✅ закрыто, в проде.
2. **Ф9** (замок формы) — ✅ закрыто.
3. **Ф10** (свёртка интерфейса `code`/`description` + удаление `note`) — ✅ **СДЕЛАНА
   2026-07-24** (миграция `0006`, обратима на снимке прода; сюита 346, фронт зелёный;
   браузер за Иваном). Фундамент стройности: дальше всё новое (Ф5/Ф6) пишется уже на
   новом интерфейсе.
4. **Ф3** (Item: три оси + `design_item_id → code` + матрица цены) — ✅ **ЗАКРЫТА
   2026-07-25**: Ф3a (`0007`) + добор (`0008`) + Ф3b (`0009`). Отдельный заход по Item,
   своими миграциями (отдельно от Ф10).
5. **Ф4** (contractor у Закупки) — ✅ сделана 2026-07-24 (`0005`). Питает Ф6.
5a. **Ф2** (единый пикер) — ✅ **СДЕЛАНА 2026-07-25**: `core/useTypeahead` + `Picker`;
   снято 11 `<select>` (изделие 6 / контрагент 4 / заказ 1) плюс эталонный
   `AddComponent` переписан обёрткой — 12 мест на общем пикере; автовыбор первого
   снят. Каталог `core/` заведён — первый камень раскладки Ф7.
6. **Ф2** (`ItemPicker`) — ⏭ **следующая**: независима, но нужна до Ф5/Ф6, чтобы новые
   формы сразу строились на ней. Пикер теперь фильтрует по `code`+`description`
   (после Ф3b переписывать его не придётся — ради этого порядка Ф3b и шла первой).
6a. **Ф12** (канон формы) — ⏭ **следующая**, принята 2026-07-26. Идёт **перед Ф5**:
   Ф5 переписывает форму закупки с нуля, канон после неё = верстать дважды. Заодно
   удешевляет Ф7 (все формы через один `FormShell` → вторая тема почти бесплатна).
7. **Ф5 + Ф13** (форма закупки + охват проектов) — самый крупный функциональный кусок;
   идут одним заходом, разрез Ф5a (охват + проекция) / Ф5b (аккордеон + пеггинг).
8. **Ф6** (Заказ → УПД) — опирается на `contractor` из Ф4.
8a. **Ф14** (снос MTI: 7 таблиц ордера → 1) — после Ф6, **до выката хвоста миграций**,
   чтобы прод получил устоявшуюся схему одним прогоном.
9. **Ф7** (шов + тема) — последним по коду, но **правило «движок отдаёт смысл»
   действует с первой строки волны**: всё новое пишем уже без протечек.

Миграции: Ф10 — одна большая (схема + данные) на все сущности, кроме Item (`0006`);
Ф3 — три свои по Item (`0007` оси, `0008` инвариант, `0009` rename); Ф4 — тривиальная
(одно nullable-поле, `0005`); Ф12a — `Attachment.label`→`description` (`0010`);
Ф12b — владельцы вложений (`0011`: 4 новых nullable-FK + пересборка
`attachment_exactly_one_owner`); Ф13 — охват закупки (`0012`: M2M
`Procurement.projects`); Ф14 — снос MTI (`0013`: 6 колонок в родителя + 5 CHECK,
снос семи детских таблиц). На проде стоит хвост по Ф1 включительно;
накопленное после него (`0005`…`0013`) поедет **одним осознанным прогоном в конце волны**
([[predeploy-culture]], большая чистка).

---

## Verify

- Бэкенд: `DB_USER=root DB_PASSWORD=root manage.py test plume` — зелёная сюита
  (сейчас 334). Новое покрытие: миграция статусов, `item_usage_in_project`,
  пеггинг в явный заказ, `create_receipt_from_purchase` (в т.ч. повторный вызов на
  остаток), цена у `posted` изделия.
- Фронт: `tsc` / `vite build` / `oxlint` — ноль новых warning.
- Сквозной прогон на реальных вьюхах: закупка → нарезка по проектам → заказ → УПД
  из заказа → приход → строка заказа закрыта.
- Браузерный клик и приёмка — за Иваном.
- Журнал: запись по итогам волны в `docs/JOURNAL.md`.

---

## Что осознанно НЕ в этой волне

- Полноценная вторая тема (только скелет — Ф7); доводка — волна 20.
- `LotPicker` (пикеры лотов) — после того, как устоится `ItemPicker`.
- Импорт BOM из Altium — живёт в [WAVE18.md](WAVE18.md), не потерян.
- Нетинг при разузловании до листьев (долг волны 16).
- Пересмотр модели фиксации изделия — после Ф3, отдельным разговором.
