# AUDIT1 — предрелизный аудит консистентности (2026-07-30)

Полный аудит проекта перед выкатом: схема · движок · вью · README · UI-доки ·
deploy · docs-хозяйство. Проведён чтением **всего** кода (models/engine/views/admin/
settings целиком, все вью фронта, оба UI-дока, README построчно против моделей)
плюс полным прогоном проверок. Ничего не правилось — только фиксация.

**Как пользоваться файлом.** Каждая находка имеет ID (`Б1а-3` = раздел Б1а, пункт 3).
Отрабатывая находку — вычёркивай (`~~…~~` + строка «закрыто: дата/коммит»).
Файл рассчитан на чтение с холодного старта: каждая находка самодостаточна
(файл:строка + суть + почему это важно).

---

## Карта файла

| Раздел | Что там |
|---|---|
| [Резюме](#резюме--вердикт) | вердикт «ок/не ок» + что правим сейчас, что потом |
| [0. Прогоны](#0-прогоны-факты) | результаты тестов/линтов/сборки |
| [А. Хорошее](#а-хорошее) | что стройно и переиспользуемо — беречь при чистках |
| [Б1а. Баги/костыли — лёгкие](#б1а-баги-и-костыли--править-легко-12-сессии) | взять и поправить, 1–2 сессии на всё |
| [Б1б. Баги/костыли — глубокие](#б1б-баги-и-костыли--глубокие-волна) | требуют волны (нашёлся один кандидат) |
| [Б2а. Вью — поправить сразу](#б2а-вью--поправить-сразу) | механические несоответствия канону — ЗАКРЫТ 30.07 |
| [Б2б. Вью — посмотреть глазами](#б2б-вью--посмотреть-глазами-и-подумать) | требуют решения Ивана |
| [Б3. README vs код](#б3-расхождения-readme-и-кода) | 9 расхождений текста; диаграммы чистые |
| [Б4. UI-доки: канон vs легаси](#б4-ui-доки-что-действует-что-легаси) | вердикт по UI_GUIDE / UI_PRINCIPLES / FIELD_MATRIX — ЗАКРЫТ 30.07 |
| [Б5. Зрелость сквозных решений](#б5-зрелость-сквозных-решений-описание--схема--движок--вью) | детские места по оси описание–схема–движок–вью |
| [В. Docs-хозяйство и deploy](#в-docs-хозяйство-и-deploy) | что отцепляем в архив; гигиена деплоя |

---

## Резюме — вердикт

**Проект зрелый и к выкату пригоден.** Три опоры (схема · движок · вью) согласованы:
ER-диаграммы README совпадают с `models.py` поле-в-поле (включая порядок), движок —
единственный писатель склада, вью красит смысл, не считает его. 456 бэкенд-тестов
зелёные, tsc/build зелёные, секретов в репо нет. Глубоких архитектурных мин **не
найдено** — единственный кандидат на отдельную волну (форма комплектации) и так
известен и запланирован.

**Правим сейчас (до выката) — ЗАКРЫТО ЦЕЛИКОМ:**
- ~~Б1а целиком — 10 мелких находок~~ — **закрыто 2026-07-30**, разбор в шапке Б1а.
- ~~Б3: синхронизация текста README~~ — **закрыто 2026-07-30** (все 9 пунктов).
- ~~Б4: разметить легаси в UI-доках~~ — **закрыто 2026-07-30**, разбор в шапке Б4.
- ~~Б2а: вью-механика~~ — **закрыто 2026-07-30** (взято в ту же сессию, что Б4),
  разбор в шапке Б2а.

**Прогоны после отработки Б4+Б2а (2026-07-30):** бэкенд не трогали (диф — только
`docs/` и `frontend/src/`), последняя цифра остаётся **458 тестов, OK**;
`tsc --noEmit` чисто; `vite build` чисто (366 КБ js / 22.3 КБ css — рост от новых
ролей и их комментариев); oxlint — те же 14 предупреждений, **все** шум
`react(only-export-components)`, живых нуль.

**Довзято перед выкатом (2026-07-31, оценка остатка):** Б2б-4 и Б2б-5 (оба вопроса к
движку — оказались дешевле, чем «посмотреть глазами»: одна правка `update_document` +
один `elif`, разборы в самих пунктах) и В-2 (линт блокирует деплой). Прогоны после:
**464 теста, OK**; `tsc`/`vite build` чисто (370 КБ js / 22.5 КБ css); `npm run lint` —
ноль; `deploy/preflight.sh` сквозным прогоном пройден.

**Правим потом (после выката, по готовности):**
- Б1б-1 — волна комплектации (уже в BACKLOG, подтверждаю необходимость).
- Б2б — вью-вкусовое: клик-проход по шести ордерам (Б2б-1), нативные `confirm()`
  (Б2б-2), ленивая загрузка фидов (Б2б-6). Требует глаз Ивана. ~~судьба экрана
  синхронизации~~ — **Б2б-3 закрыт 2026-07-31** (переодет в `FormShell`);
  ~~два вопроса к движку~~ — **Б2б-4/-5 закрыты 2026-07-31**.
- Б5 — наблюдательный список (конкуренция, N+1 на списках), не действия.
- В-1 — архивация docs/ (после сквоша миграций, одним движением).

~~**Решения, которые нужны от Ивана до сквоша миграций:** судьба `BomLine.position`
(Б1а-3) и `StockMovement.Type.RETURN` (Б1а-2) — оба «снести или узаконить».~~
**Решено 2026-07-30: оба снести** — сделано миграцией `0018`. Путь к сквошу свободен.

---

## 0. Прогоны (факты)

- **Бэкенд:** `manage.py test plume` против Docker MySQL 8.0.25 — **456 тестов, OK**
  (15.7 s). `System check identified no issues`.
- **Фронт:** `tsc --noEmit` — чисто; `vite build` — чисто (366 КБ js / 21.6 КБ css).
- **oxlint** (`npm run lint`): ~20 предупреждений. Из них шум — `react(only-export-components)`
  (fast-refresh, status.tsx/FormHeader.tsx); **живых два**: см. Б1а-7.
- Побочно замечено: при прогоне миграций (в т.ч. на каждом тест-ране) в stdout
  печатается `Item: 0 из 0 → locked=True …` — см. Б1а-9.

---

## А. Хорошее

Короткий список того, что прошло через весь проект стройно. Это не комплименты, а
**реестр инвариантов, которые надо беречь** при любых чистках.

1. **Один писатель склада.** `engine.rebuild_movements` — единственная точка записи
   `StockMovement`; гейт Ф15 («замок гейтит склад») врезан ровно в неё, поэтому
   остатки/дефицит/бюджет/карта подчиняются правилу автоматически. Админка честно
   read-only по движковому (`_ReadOnlyAdmin`), с внятным объяснением границы в
   docstring [admin.py](../backend/plume/admin.py).
2. **Двухслойные инварианты.** Каждое правило живёт дважды: CHECK в БД (страховка от
   прямого ORM/админки) + прикладной гейт с дружелюбной ошибкой
   (`purchase_locked_has_contractor` + `lock_purchase`; `item_locked_has_category` +
   `lock_item`; `_require_header`). Урок Ф12e («CHECK без гейта — это 500 в лицо»)
   выучен системно.
3. **Одна таблица ордера + proxy-виды.** `StockDocument` + `_KindManager` — снос MTI
   пережили 108 обращений `Receipt.objects…` без правки. Единая дорога рождения
   (`_born_order` + `fallback_code` + `require_unique_code`), единая правка шапки
   (`update_document` + `_UNSET`-часовые), единое удаление (`delete_stock_document`).
4. **Единая ось `locked` на пяти сущностях** — одно поле, один глагол, одни
   эндпойнты `lock/unlock`; сильная у документов (арифметика), слабая у справочников
   (заморозка). Ни одного второго словаря статуса не осталось.
5. **`code`+`description` везде** — включая фолбэк «{Вид} {id}», мягкую уникальность
   и пустой титул как «подталкивание». Ноль исключений в живом коде.
6. **Шов движок↔вью выдержан.** Движок отдаёт смысл (`contractor_mismatch`,
   `counterparty_sides`, `project_health`, `supply/shipment = интеграл или None`),
   вью выбирает знак. Тема: `models.THEMES` (слаги) → `engine.set_theme` (страж) →
   `core/theme.ts` (применение) → `themes/*.css` (значения) → `registry.ts` (ярлыки) —
   образцовая слоистость, ни одна сторона не знает лишнего.
7. **Один лэйаут форм.** `FormShell` + `OrderFields` + `useOrderForm` +
   `Field/TextField` + единый `Dropdown`/`Picker`/`AnchoredMenu` (порталы) — шесть
   видов ордера сведены к диспетчеру на 55 строк ([OrderForm.tsx](../frontend/src/OrderForm.tsx)).
   «Неполированные» вью (Transfer/Location/Writeoff/Requisition/Inventory/Relocation)
   на деле **уже на каноне** — отстаёт только тело комплектации (см. Б1б-1).
8. **CSS-иерархия по заветам:** структура в `index.css` токенами, значения в темах,
   приглушение — инверсией умолчания (`table.grid td` + роли `.c-key/.num/.code`),
   171 использование `var(--…)`. Новая тема = один файл + строка реестра — проверено
   светлой темой.
9. **ER-диаграммы README = models.py буквально.** Сверено поле-в-поле, включая
   nullable-нотацию `|o` и порядок полей. Расхождений ноль. Правило «диаграммы —
   источник правды» работает.
10. **Гигиена деплоя.** `preflight.sh` (секреты/миграции/тесты/сборка) вшит в
    `deploy_local.sh`; секреты — аргументами; греп по хостам/адресам/ключам чистый;
    `.env` и `deploy/snapshots/` в `.gitignore` (проверено `check-ignore`).
11. **Тесты названы поведением** (Ф16), докстринги переписаны, покрытие сверялось
    при чистке. Сюита реально ловит регрессии (поймала `origin__receipt__purchase`).
12. **Комментарии-журнал в коде.** `models.py`/`engine.py` читаются как документация
    решений с «почему». Это осознанная ставка проекта; её цена — устаревание
    (см. Б1а-6, Б2а-4) — но баланс сейчас сильно в плюс.

---

## Б1а. Баги и костыли — править легко (1–2 сессии)

> **ЗАКРЫТ ЦЕЛИКОМ 2026-07-30** (сессия «отработка аудита-1»). Как закрыт каждый:
> 1 — `procurements` POST обёрнут как соседи; 2 — `RETURN` снят из `choices`
> (решение Ивана: возврат = знак, не тип), README тоже; 3 — `BomLine.position`
> снесён из схемы/движка/API/диаграммы (решение Ивана: исполнить приговор
> FIELD_MATRIX A1); 4 — `written_off_lots` фильтрует `document__locked=True`
> (+ тест); 5 — новый `_require_no_closing_draft`: лот в черновом закрывающем акте
> → дружелюбный отказ с кодом акта вместо второго документа (+ тест); 6 —
> докстринги `library_diff`/`apply_library_diff` приведены к инварианту 0008;
> 7 — оба живых предупреждения ушли, заодно почин stale-выбора лота в ghost-строке
> (выбор сверяется со свежим списком кандидатов); 8 — `'ATOMIC_REQUESTS': True`;
> 9 — миграция `0004` отчитывается только когда есть о чём (как `0003`);
> 10 — комментарии TransferView/LocationView/settings.py приведены к факту.
> Схема поехала одной миграцией `0018_audit1_drop_bomline_position` (п. 2 + 3).

1. ~~**500 на создании закупки с занятым кодом.**~~ [views.py:1779-1785](../backend/plume/views.py#L1779-L1785) —
   единственный POST-создатель без `try/except ValidationError`:
   `engine.create_procurement` зовёт `require_unique_code`, и занятый `code` в теле
   запроса уронит вьюху в 500 (Django-ValidationError DRF не конвертирует). Все
   остальные create-вьюхи обёрнуты. Из UI не воспроизводится («＋ Новая» шлёт пустое
   тело), но это дырка в едином паттерне. *Фикс: обернуть как соседей.*
2. ~~**`StockMovement.Type.RETURN` — мёртвый enum.**~~ [models.py:815-818](../backend/plume/models.py#L815-L818).
   `rebuild_movements` пишет только RECEIPT/ISSUE; RETURN не создаётся нигде
   (греп по движку — ноль вхождений). При этом README обещает «компенсирующий
   `RETURN`» (см. Б3-3). *Решить: снести значение (миграции не надо — это choices)
   или узаконить сценарий. Решение — до сквоша.*
3. ~~**`BomLine.position` — приговорённое поле живёт.**~~ FIELD_MATRIX (A1) и JOURNAL
   2026-07-05: «мёртвое поле → удалить из схемы перед релизом». Факт: поле в схеме
   ([models.py:377](../backend/plume/models.py#L377)), в движке (`add_bom_line`),
   в API (`api.ts:663-665`) — а форма изделия его **не показывает** (греп по
   ItemView — ноль). *Решить судьбу до сквоша миграций: снести или вывести на форму
   (позиционное обозначение — вообще-то полезная вещь для плат).* 
4. ~~**Черновое списание уже предлагает ре-материализацию.**~~
   [engine.py:3812-3835](../backend/plume/engine.py#L3812-L3835) `written_off_lots`
   суммирует строки списаний **без фильтра `document__locked`** — лот из чернового
   акта уже виден в пикере инвентаризации как «списанный, можно вернуть». Расходится
   с границей Ф15 («черновик — намерение, не факт»), которую держат все соседние
   интегралы (`_counterparty_supply`, `_project_spent`, `_line_received`).
   *Фикс: `+ document__locked=True` в оба фильтра.*
5. ~~**Даблклик в панели закрытия плодит второй акт.**~~ [engine.py:2323-2361](../backend/plume/engine.py#L2323-L2361)
   `writeoff_lot`/`requisition_lot`: если лот уже лежит в черновом акте, повторный
   клик **создаёт новый акт** с тем же остатком — после фиксации обоих лот уйдёт в
   минус. Панель показывает `closing_drafts`, но от повторного клика не защищает
   (после Ф15 остаток на панели не гаснет до фиксации — клик «не сработал» выглядит
   правдоподобно). *Фикс: лот уже в черновом закрывающем акте → дружелюбный отказ
   («остаток уже в акте X — зафиксируйте его»), а не новый документ.*
6. ~~**Докстринг `library_diff` врёт про замок.**~~ [engine.py:4135-4143](../backend/plume/engine.py#L4135-L4143)
   обещает «locked не трогаем — решение Ивана 2026-07-24», а `apply_library_diff`
   (правильно, по инварианту 0008 `synced ⟹ not locked`) замок **снимает**.
   *Фикс: переписать докстринг.*
7. ~~**Два живых предупреждения oxlint.**~~
   [KittingView.tsx:162](../frontend/src/KittingView.tsx#L162) — `useEffect` со
   сложным выражением в deps и missing dep `lots` (потенциальный stale-выбор лота в
   ghost-строке — возможно, родня бага Б1б-1);
   [LibraryImportView.tsx:68,74](../frontend/src/LibraryImportView.tsx#L68) —
   `no-unused-expressions`. *Фикс: довести линт до нуля; опционально — включить
   `lint` в preflight (сейчас preflight гоняет oxlint, но предупреждения не валят).*
8. ~~**`ATOMIC_REQUESTS` не включён.**~~ Многошаговые мутации движка идут без
   транзакции: `add_requisition_line` (строка + лот + 2 rebuild),
   `lock_kitting` (лот + замок + rebuild), `delete_stock_document` (файлы + лоты +
   каскад). Обрыв посередине оставит полусостоявшуюся операцию. Явные `atomic` есть
   только у `create_with_fallback_code`/`apply_library_diff`/`rollup`.
   *Фикс: `'ATOMIC_REQUESTS': True` в settings — одна строка, закрывает класс;
   синхронный движок без Celery ей ничем не мешает.*
9. ~~**Миграция печатает в stdout.**~~ Каждый `migrate`/тест-ран выводит
   `Item: 0 из 0 → locked=True …` (data-шаг миграции статусов). *Фикс: печатать
   только при `verbosity >= 2` или снести вместе со сквошем.*
10. ~~**Косметика стухших комментариев.**~~ [TransferView.tsx:4-5](../frontend/src/TransferView.tsx#L4-L5)
    («Мягкого замка нет — правимо всегда» — замок есть с волны 13, код ниже сам себя
    опровергает); [LocationView.tsx:4-5](../frontend/src/LocationView.tsx#L4-L5)
    («Удаления нет» — `del` реализован тут же);
    [settings.py:1-11](../backend/config/settings.py#L1-L11) («Generated by
    django-admin 5.1.15», комменты «Django 5.1+» при факте 6.0.6).

## Б1б. Баги и костыли — глубокие (волна)

Хорошая новость: **архитектурных мин не найдено.** Схема после волны 19 не несёт
костылей под движок, движок не несёт логики, которой место в схеме. Единственный
кандидат на волну — известный:

1. **Форма комплектации — глубокий проход (уже в BACKLOG, подтверждаю).**
   Аудит подтверждает оба слоя долга:
   *(а) вью* — ~~единственное тело формы не на каноне таблиц: своя вёрстка `.kit-comp`
   вместо `table.grid` с `c-key/c-desc`, свой `QtyInput` вместо `CommitInput`~~ —
   **закрыто 2026-07-31** на клик-проходе (Иван: «на вкладке Строки какой-то мусор,
   пусть будет синхронно с закупочным контуром»). Тело уехало на одну `table.grid`:
   компонент — строка (глиф `ItemGlyph` + `c-key` + `c-desc` с многоточием + надо/ед./
   пробито/остаток), пробитые партии и призрак — ступень `.c-key.ind`, `QtyInput`
   заменён общим `CommitInput`, в проекцию добавлены оси компонента под глиф. Своих
   классов не заведено ни одного — роли уже были; сняты мёртвые `.kit-comp*` и `.device`.
   Остаётся свой локальный `api.items()` при том, что `OrderForm` уже владеет
   справочником;
   *(б) подозрение на stale-state* — BACKLOG-репорт Ивана «шапка показывает "в
   работе", а ошибка — "документ зафиксирован"»; кандидат в корни — эффект
   [KittingView.tsx:162](../frontend/src/KittingView.tsx#L162) (Б1а-7) и то, что
   ghost-строка держит `lotId` в локальном стейте поверх меняющейся формы.
   *Движковая* сторона комплектации при этом здорова (lock/unlock/guards чистые,
   тестов много) — волна нужна фронтовая, с одним заходом на пробивку.

---

## Б2а. Вью — поправить сразу

> **ЗАКРЫТ ЦЕЛИКОМ 2026-07-30** (сессия «Б4 + Б2а»). Общий ход: каждая находка
> превратилась не в правку по месту, а в **роль** — класс или токен, который дальше
> работает сам. Что заведено в `index.css`: токены `--in-num` (72px, число в строке) и
> `--col-txt` (160px, колонка свободного текста из документа); роли `.c-txt` (колонка
> part number / названия из УПД / имени в накладной), `.c-key.ind` (ступень
> вложенности), `.prow--empty` (пустое состояние строкой), `.lbl` (служебная подпись —
> в `.kit-actions` и `.prow`), `.sub` (вторичное значение внутри полноцветной колонки —
> класс наконец получил правило), `.lib-diff` (три правила экрана синхронизации);
> правило соседства команд `td.act > * + *:not(.hint)`.
> Пропсы `width` **снесены совсем** — и у `CommitInput`, и у `Picker`: они ставили
> inline-стиль, который сильнее любого класса (это и был корень разъезда 56/60/72).
> Инлайн-стиль во всех вью остался **ровно один** — `paddingLeft` дерева BOM
> ([ProjectView.tsx](../frontend/src/ProjectView.tsx)), где значение вычисляется из
> глубины узла; он откомментирован как законное исключение.
> Заодно: `.pn` в ячейках заменён на `.code` (6 форм), моно снято с заголовка `.uom`,
> мёртвые правила `h1.title .pn/.lit` удалены — это BACKLOG «три мёртвых класса».
> Прогоны: `tsc` чисто, `vite build` чисто, oxlint без новых предупреждений.
> **Принято Иваном в браузере 2026-07-30** («потыкал интерфейс, всё хорошо») — в том
> числе спорные места: «имя в накладной» на передаче в 160px не тесное, колонка команд
> в «Привязке» не разъехалась. Ступени ширин (кол-во 72 / текст 160) закреплены;
> захочется иначе — это правка токена `--in-num` / `--col-txt`, а не возврат инлайна.

1. ~~**Пиксельные ширины в строках форм.**~~ `width={56..240}` у `CommitInput` и
   `style={{width}}` у инпутов ghost-строк — Receipt/Inventory/Transfer/Requisition/
   Relocation/ItemView/ProjectView (полный список — греп `width={` по `src/`).
   Канон §13.3/§7a: ширина — токенами/ролями колонок, инлайн сильнее класса и
   выбивает поле из сетки. *Свести к классам (`.qty-in` уже есть; завести роли
   `.in-pn`/`.in-name` или отдать ширину колонке).*
2. ~~**Инлайн-стили цветов/отступов вместо ролей**~~ — россыпь
   `style={{ color: 'var(--fg-dim)' }}` и `marginTop/marginLeft` в
   [ItemView.tsx:326-330](../frontend/src/ItemView.tsx#L326-L330),
   [KittingView.tsx:119](../frontend/src/KittingView.tsx#L119),
   [PeggingPanel.tsx](../frontend/src/PeggingPanel.tsx),
   [ScopeDeficitPanel.tsx](../frontend/src/ScopeDeficitPanel.tsx),
   [ProjectView.tsx:361-362](../frontend/src/ProjectView.tsx#L361-L362). Ось
   «идентичность/контекст» уже выражена умолчанием — эти инлайны либо дублируют её,
   либо должны стать ролью.
3. ~~**Мёртвые/непоследовательные классы**~~ `.sub` / `.pn` в ячейках / моноширинный
   заголовок `.uom` — уже описаны в BACKLOG («Три мёртвых класса», волна 21 Ф3).
   Закрыть одним заходом вместе с п.1–2.
4. ~~**LibraryImportView — самый внеканонный экран.**~~ Пачка инлайн-стилей
   ([LibraryImportView.tsx:99-193](../frontend/src/LibraryImportView.tsx#L99-L193)),
   собственная вёрстка, и по диагнозу §13.10 это «кастомный экран, прибитый к
   сайдбару» (`extraTop` в Компонентах). Минимум — вычистить инлайны; вопрос
   «легализовать или переодеть в FormShell» — в Б2б-3 (**переодет 2026-07-31**).

## Б2б. Вью — посмотреть глазами и подумать

Требуют решения Ивана; аудит только намечает путь.

1. **Путь доводки «непотыканного» короче, чем кажется.** Комплектации, передачи,
   склады, требования, списания, инвентаризации, перемещения — **скелет у всех уже
   канонический** (FormShell/OrderFields/табы/мета/LotGlyph). Реальный остаток:
   тело комплектации (Б1б-1) + вкусовой слой, который Иван собирался собрать сам
   («зачистка мелких правок UI», BACKLOG). Предлагаемый порядок: сначала Б2а
   (механика), потом совместный клик-проход по шести ордерам со списком мелочей,
   волной — только комплектация.
2. **Нативные `confirm()`/`alert()` посреди темизированного UI.** Удаления, расфиксация,
   предупреждение о пустом коде ([FormShell.tsx:65-71](../frontend/src/FormShell.tsx#L65-L71)) —
   системные диалоги ОС. Функционально честно, стилистически чужеродно. Подумать:
   свой лёгкий confirm в теме (один компонент, много точек) — или осознанно оставить
   (нативный диалог не притворяется частью продукта). Решение вкусовое.
3. ~~**Три экрана вне канона §13.10:**~~ **ЗАКРЫТО 2026-07-31 — переодели в
   `FormShell`.** Развилка «легализовать как исключение или переодеть» решена третьим
   способом: у мастера нет полей и нет фиксации, и это оказалось не препятствием
   канону, а содержанием формы — зону полей занимает инструкция, замок правит описания
   классов в новом табе «Категории», статусы дифа разведены по четырём табам.
   `h1.title`/`.subtitle` снесены из темы. Осталось: «Аккаунт» (Sel без Mode — легален,
   обоснование в App.tsx) и Login (вне канона по природе). Подробности — в JOURNAL
   (`07-31`).
4. ~~**`_require_number` запрещает очистить номер в черновике**~~ — **закрыто
   2026-07-31.** `_require_number`/`_require_date` снесены совсем: пустая строка в
   PATCH теперь **очищает** поле (`''` / `NULL`), обязательность остаётся на фиксации
   (`_require_header` + `REQUIRED_HEADER_BY_KIND`). Взято шире находки — по стандарту
   консистентности форм: дата страдала тем же и вдобавок расходилась с заказом, где
   `update_purchase` очистку уже разрешал. Дата идёт мимо `_apply` (там `None` занят
   под «не передано»), как в `update_purchase`. Тест
   `test_draft_header_clears_but_lock_still_demands_it`; старая проверка «пустой номер
   → отказ» в `test_transfer_header_edit_and_lock` снята.
5. ~~**Blocker-текст панели закрытия**~~ — **закрыто 2026-07-31.** При смеси панель
   называет обе половины работы: «В черновиках X, не разобрано Y — зафиксируйте
   черновики и сведите остаток в 0». Числа считаются от магнитуды расхода черновиков
   (`_closing_drafts.qty`) и уходят в payload отдельными полями
   (`residual_in_drafts` / `residual_unsorted`) — вью может показать их таблицей, не
   разбирая строку. Когда черновики покрыли остаток целиком, текст прежний (второе
   число было бы нулевым шумом). Фронт не трогали: `blocker` рендерится как есть.
   Тест `test_closure_blocker_names_both_halves_of_work`.
6. **Старт приложения грузит 13 фидов разом** ([App.tsx:132-157](../frontend/src/App.tsx#L132-L157)).
   Сейчас дёшево; при росте данных первым столбом встанет `/api/items/` (см. Б5-3).
   Ленивая загрузка по режиму — кандидат «когда заболит», не сейчас.

---

## Б3. Расхождения README и кода

> **ЗАКРЫТ ЦЕЛИКОМ 2026-07-30.** Все девять текстовых расхождений сведены с кодом;
> в диаграммах тронуты ровно два места, и оба — следствие правки схемы (снятые
> `BomLine.position` и `RETURN`), плюс «uniq в рамках заказа» (п. 8). П. 4
> (`verify`-команда) закрыт пометкой *(план)* — команду не писали, это новая
> функциональность, а не синхронизация текста; п. 5 урезан до факта (снимок
> `unit_cost` берётся один раз, кнопки и подсветки расхождения нет).

Диаграммы (продуктовая + техническая) — **чистые**, сверены поле-в-поле (А-9).
Все находки — в **тексте** «Ключевых принципов»: волны 15–19 доехали до глоссария и
диаграмм, но не до старых буллетов. README теперь ⅓ ДНК — эти пять абзацев стоит
поправить до выката.

1. ~~**«Строка комплектации сразу постит движение» — отменено Ф15.**~~
   [README.md:414-424](../README.md#L414-L424): «строка … **сразу постит движение**
   (ISSUE компонента … ) — так комплектацию фиксируем по ходу». С Ф15 черновик не
   двигает склад вообще; тот же README ниже (:467-474) описывает это правильно.
   Внутреннее противоречие двух буллетов — обновить буллет про комплектацию
   (история пайки осталась, немедленного движения нет).
2. ~~**«Поставщик УПД приезжает из `Procurement.contractor`» — отменено Ф17.**~~
   [README.md:576-583](../README.md#L576-L583). Код берёт `purchase.contractor`
   ([engine.py:1329-1375](../backend/plume/engine.py#L1329-L1375)); глоссарий
   (:95) уже говорит правильно («поставка копирует контрагента заказа»). Поправить
   буллет «Заказ → УПД».
3. ~~**«Компенсирующий `RETURN`»**~~ ([README.md:424](../README.md#L424)) — тип
   существует только как значение enum, движок его не пишет (Б1а-2). Либо снести из
   текста и enum, либо признать планом с пометкой.
4. ~~**«Периодическая `verify`-команда (cron)»**~~ ([README.md:459-461](../README.md#L459-L461))
   описана как существующая — команды нет (`management/commands/` = только
   `seed_demo`). CLAUDE.md честно говорит «cron/verify пока не заведены». Пометить
   как план или (лучше) написать команду — `rebuild_all` + сверка уже есть, обвязка
   на полчаса.
5. ~~**Кнопка «обновить по акту изготовления» с диффом**~~
   ([README.md:428-436](../README.md#L428-L436)) — не существует. Снимок
   `unit_cost` делается один раз на `lock_kitting` (`_device_unit_cost`); подсветки
   расхождения снимка с текущей суммой нет. Раздел описывает нереализованное как
   сущее — пометить или урезать до факта.
6. ~~**«Переоткрытие узла с потомками — с предупреждением, не запрет»**~~
   ([README.md:496-500](../README.md#L496-L500)) — фактически движок **запрещает**
   (`unlock_kitting` отказывает при потреблённом лоте, [engine.py:1152-1162](../backend/plume/engine.py#L1152-L1162)).
   Код строже и, по-моему, правильнее README — обновить текст под факт.
7. ~~**Терминология `KittingLine`**~~ в принципах (:417, :636, :646, :650, :671, :719) —
   таблица снесена в волне 13 (единый `StockLine`); глоссарий это знает, принципы
   пишут старым именем. Заменить на «строка пайки (`StockLine`)» или подобное.
8. ~~**`PURCHASELINE` — «uniq в рамках закупки»**~~ ([README.md:329](../README.md#L329)) —
   после разделения терминов это «в рамках **заказа**». Одно слово, но по нынешнему
   словарю оно значит другую сущность.
9. ~~**«Быстрый старт» — заглушка**~~ (:28-35) с июня. Для «README = инструкция, по
   которой можно вспомнить и доработать» это дырка №1: локальный старт (Docker MySQL
   + venv + два dev-сервера) и ссылка на deploy/README — полстраницы.

## Б4. UI-доки: что действует, что легаси

> **ЗАКРЫТ ЦЕЛИКОМ 2026-07-30** (сессия «Б4 + Б2а»). Сделано ровно то, что предлагал
> аудит, — разметка, а не переписывание:
> 1. **UI_GUIDE стал единственным источником правды по интерфейсу.** Снят статус
>    «черновик на вычитке»; заведён **§0** («Северные звёзды и анти-паттерны»,
>    автосейв, цвет=смысл со словарём статусов, §0.5 «Консистентность» — бывшие
>    Правила 2 и 3); **§9** «Действия» закрыт решением задним числом (две поверхности:
>    колонка контролов шапки + глиф-команда строки; правый клик не вводим), туда же
>    переехало Правило 4 как **§9.1**; **§11** «Чистовой проход» — все галки
>    проставлены с пояснениями, секция помечена исторической. Ссылки «UI_PRINCIPLES §7»
>    в коде (`themes/*.css`, `Picker.tsx`) переписаны на «UI_GUIDE §0.4».
> 2. **UI_PRINCIPLES помечен архивом**: шапка-вердикт с таблицей «что куда переехало» +
>    списком отменённого жизнью, и пометки **по месту** у семи устаревших разделов
>    (статус-бар, правый клик, Правило 1, модалки, режим изоляции, таблицы конформанса).
>    Правки в него больше не вносим.
> 3. **FIELD_MATRIX помечен архивом**: роль выполнена, очередей не осталось, порядок
>    полей держит §13.4a + ER-диаграмма, редактируемость — код `update_*`.
> Заодно вычищены два стухших хвоста самого UI_GUIDE: «остаток очереди #A» (закрыт
> ещё Ф2j/Ф2k) и «поправить в UI_PRINCIPLES при чистовом проходе».

Эталон — фактический вид полированных вью (решение Ивана на этом аудите).

**UI_GUIDE.md — живой канон.** §1 (словарь), §3 (типографика), §5 (замок/фиксация),
§6a/6b (пикер/дропдаун), §7a (вёрстка строк), §8, §10 (темы), §12, §13 (канон формы) —
совпадают с кодом; это лучший UI-док проекта. Внутренние хвосты:
1. Статус в шапке — «черновик на вычитке» — давно неправда: это принятый канон.
2. **§11 «Чистовой проход» — стухший чеклист**: Codicons, Inter, палитра, единый
   заголовок, замок, шаблон списков, ⌘K, снятая строка состояния — всё давно
   сделано, а галки пустые. Либо проставить и закрыть секцию, либо снести (история
   есть в JOURNAL).
3. **§9 «Действия» — вечное `[ОТКРЫТО]`**: «инвентарь действий» так и не собран, а
   ответ де-факто устоялся (колонка команд шапки + глифы-команды строк, ≤3 — на
   месте). Закрыть решением задним числом, правый клик не вводить.
4. §2 (таблица режимов) — сам помечен как исторический слепок; ок.

**UI_PRINCIPLES.md — на ⅔ легаси.** Живое: северные звёзды, анти-паттерны,
автосейв (§2), цвет=смысл (§7), Правило 4 (2026-07-26, свежее и важное).
Отменённое жизнью:
- статус-бар (§2, «Каркас») — упразднён UI_GUIDE §1;
- «Правило 1»: форма создания + кнопка «Создать» — отменено Ф12e (born-by-click);
- «модалка для создания мелкой сущности» (§8) — теперь `onCreate` пикера, без модалок;
- «якоря выбираются при создании и инлайн не правятся» — отменено Ф2k (якоря ⚓
  правятся под замком);
- контекстное меню по правому клику как «основная поверхность» — не случилось и
  не планируется (§9 UI_GUIDE);
- «режим изоляции в проект» — не существует (его роль забрали таб «Склад» формы
  проекта и фильтры списков);
- таблицы конформанса Правил 1–3 — устарели (WAVE14 всё закрыл);
- `○` синий статус — в живом словаре почти не используется.
*Предложение:* перегнать 4 живых куска в UI_GUIDE (§0 «Северные звёзды и
анти-паттерны», Правило 4 — в §9), файл пометить «архив, поглощён UI_GUIDE» —
это честнее, чем держать два «источника правды по интерфейсу» (шапка
UI_PRINCIPLES до сих пор утверждает, что источник — он).

**FIELD_MATRIX.md — выполнил роль, устарел словарём** (`status {draft⇄posted}`
вместо `bool locked`, «Ф4 — отдельная сессия» про сделанное). Порядок полей теперь
держит §13.4a + ER-диаграмма, редактируемость — сам код `update_*`. В архив с
пометкой; единственное живое из него — приговор `BomLine.position` (Б1а-3).

## Б5. Зрелость сквозных решений (описание — схема — движок — вью)

Общая оценка: **взрослая**. Ось выдержана: правило рождается решением (JOURNAL),
ложится инвариантом в схему, гейтом в движок, знаком во вью — примеры Ф15/Ф17/Ф3
проходят все четыре слоя без потерь. Найденные «детские места» — не мины, а
осознанные упрощения, которым пора завести наблюдение:

1. **Транзакционность** — см. Б1а-8 (`ATOMIC_REQUESTS`). Единственное место, где
   упрощение может молча испортить данные.
2. **Конкуренция двух операторов.** Прод многопользовательский, а версий/оптимистичных
   блокировок нет: два человека в одной форме перетирают друг друга по last-write-wins,
   и автосейв это ускоряет. Для команды в несколько человек — приемлемо; знать и не
   удивляться. Дешёвая полумера, если заболит: сверка `locked`-состояния в PATCH.
3. **N+1 по философии.** `/api/items/` = `item_is_used` (6 Exists) × N изделий;
   `/api/projects/` = полное разузлование `project_health` × N проектов; список
   заказов = `purchase_coverage` × строки. «Данных мало, всё свежее» — честная
   ставка MVP, но это те три точки, где рост данных проявится первым. Действие
   сейчас: замерить на снимке прода (`pull_prod`), записать базовую цифру; чинится
   точечно (annotate/prefetch), волны не потребует.
4. **Права.** Один уровень доверия («любой залогиненный — полноправный оператор»),
   `attachment_download` отдаёт файл любому залогиненному. Для внутреннего
   инструмента — осознанно и записано в settings; не долг.
5. **Фронт без тестов (0).** Держится на tsc + тонкости вью-слоя (логика в движке).
   Это правильный баланс; единственное место, где логика утекла во вью, — сборка
   `orderEntries`/фильтров в App.tsx, и она тривиальна. Не заводить тесты ради
   тестов; следить, чтобы вью не умнела.
6. **Джанго-миграции: хвост из 17.** Сквош в `0001_initial` перед 1.0 — уже принятая
   политика; аудит добавляет только порядок: сначала решения Б1а-2/Б1а-3 (RETURN,
   position), потом сквош, потом архивация docs (В-1).

## В. Docs-хозяйство и deploy

1. **Предложение по архивации docs/** (после сквоша, одним движением):
   - **живое:** `JOURNAL.md` (память), `BACKLOG.md`, `UI_GUIDE.md` (канон),
     `AUDIT1.md` (этот файл);
   - **в `docs/archive/`:** `WAVE1…WAVE18.md`, `WAVE20.md`, `WAVE21.md` (по закрытии),
     `FIELD_MATRIX.md`, `UI_PRINCIPLES.md` (после переноса живых кусков в UI_GUIDE);
     `WAVE19.md` — туда же после выката;
   - `CLAUDE.md` при этом подчистить: ссылки на WAVE1/«волну 1», раздел «Подход к
     реализации» ссылается на файлы, которые уедут в архив.
2. ~~**deploy/ — здоров** (А-10). Единственная мысль: preflight сейчас не валит деплой
   на предупреждениях oxlint~~ — **закрыто 2026-07-31.** Шум
   `react(only-export-components)` выключен в `.oxlintrc.json` (правило про Fast
   Refresh, а модуль-со-словарём — норма шва движок↔вью: `status.tsx`/`FormHeader.tsx`
   держат компонент и словарь смыслов рядом); `npm run lint` идёт с
   `--deny-warnings`; в `preflight.sh` шаг стал `bad` вместо `warn`. Теперь
   предупреждений не бывает фоновых — каждое означает живую находку и валит деплой.
   Побочно вскрылось: прежний `if npm run lint` печатал «oxlint чистый» **при 14
   предупреждениях** (oxlint выходил с 0) — то есть шаг не только не блокировал, но и
   врал. Сквозной прогон `deploy/preflight.sh` после правки — пройден.
3. **JOURNAL (5707 строк) и WAVE19 (2695)** — с «Картами файла» в шапках, читаются
   по дате; менять ничего не надо, они и есть третий уровень правды.
