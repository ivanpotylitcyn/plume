# Бэклог — Plume

Копилка **мелочей и отложенного**: то, что всплыло по ходу работы, но не тянет на
фазу волны и не должно потеряться. Заведена 2026-07-20.

**Как пользоваться:**
- Всплыла мелочь по ходу сессии — дописать сюда строкой, не отвлекаясь от текущей задачи.
- **При планировании каждой новой волны — сначала заглянуть сюда** и разобрать: взять
  в волну, оставить лежать или выкинуть как неактуальное.
- Взятое в волну — отмечать, куда уехало (ссылкой на фазу), и убирать отсюда.
- Не путать с **реестром гигиены** (`WAVE19.md`, Ф8): там устаревшие артефакты кода
  и принципов, найденные фоном. Здесь — задачи и вопросы «на потом».

---

## Отложенные волны

- **[Волна 18 — импорт BOM из Altium](WAVE18.md) — ЖДЁТ, не реализована.** Перенесена
  из волны 16 (Ф2). Заблокирована на входных данных: нужен формат и образец файла
  выгрузки. Как только образец есть — волна готова к планированию.

---

## Интерфейс

- **Заголовок колонки — по центру колонки вместо левого края?** Заголовок списка
  приведён к «первой строке» (§7a, Ф12a): тот же кегль, что у данных, выключка по
  колонке. Иван предложил попробовать **центровку заголовка** и сам же усомнился —
  отложено до момента, когда захочется посмотреть глазами. Правка ровно в одном месте
  (`table.grid th` + `.prow--head`). *Всплыло на приёмке Ф12a (2026-07-26).*

- **Полировка мелочей после приёмки Ф12c.** Канон раскатан на все формы и принят
  2026-07-27 с первого взгляда, но Иван отметил: «мелочи есть, полировка нужна, но не
  сейчас». **Список не собирали** — собрать вместе с Иваном, когда дойдут руки (скорее
  всего частично закроется Ф12d, которая и так чистит вёрстку шапок). *Всплыло на
  приёмке Ф12c.*

- **Формы без фиксации: какие контролы в шапке?** У Места и Дефицита фиксации нет
  (`UI_GUIDE.md` §5: «у справочников фиксации нет — только контурная пара замка»).
  После волны 19 контролы шапки — это карандаш/глаз (режим), замок (фиксация) и
  корзина. У таких форм замка нет вовсе → в колонке остаются два контрола. Проверить,
  что колонка не разъезжается и выглядит намеренно, а не «чего-то не хватает».
  *Всплыло при проектировании Ф9 (2026-07-20).*

---

- **Кнопка «Удалить» у проекта: нужна ли в форме или только через админку?** Замечено
  Иваном 2026-07-22 (клик по Ф9). Проверено: непустой проект защищён (guard бэка),
  пустой удаляется — это **удобно**, поэтому пока оставляем. Вопрос на подумать: не
  опаснее ли это, чем полезнее; может, удаление проектов — только через Django-админку.
  Решения не принимаем, пусть отлежится. *Всплыло на приёмке Ф9.*

- **Кнопка «Скачать» (слот шапки) — раскатать по остальным нужным местам.** Ф4 (волна 19)
  сделала переиспользуемый слот `download?: {href,title}` в `FormHeader` (серая → зелёная
  на hover, глиф `file`, в слоте корзины у зафиксированного). Иван 2026-07-24: «такую же
  добавим в остальные нужные места — куда именно, определимся позже». Кандидаты на подумать:
  выгрузки/печать у других документов (УПД, акты комплектации/передачи/списания). *Всплыло
  на приёмке Ф4.*

## Движок и модель

- **Комплектация: форма и бэкенд расходятся в мнении о замке.** Замечено Иваном
  2026-07-21 при клике по dev после Ф1c (волна 19). Наблюдаемое (скриншот): шапка
  показывает «в работе» (то есть `locked=false`), кнопка «Зафиксировать · родить
  прибор» отрисована — и **одновременно** висит ошибка «Документ зафиксирован —
  расфиксируйте для правки». Формулировка новая (мой `_require_unlocked`), значит
  след свежий. Гипотезы, **не проверены**: (а) форма не перезагружает проекцию после
  неудачной/успешной мутации и рисует stale-состояние; (б) `_require_unlocked`
  сработал не на том объекте. Диагноз по скриншоту не ставился — дешевле посмотреть
  в код, когда дойдут руки.
  **Важно:** Иван сообщил, что «там всё очень плохо» и в целом форма комплектации
  требует такого же глубокого прохода, как закупочный контур. Комплектация вне
  волны 19 (это не закупочный контур) — **осознанно отложена целиком**, чтобы не
  открывать второй фронт. Заходить туда отдельной волной, с анализом, а не
  точечными заплатками.

- ✅ ~~**Ревизия и рефактор слова «Cockpit» в коде.**~~ **СДЕЛАНО 2026-07-26.**
  Слово вычищено разом и последовательно: `*_cockpit` → `*_form` (движок, 9 функций),
  `*Cockpit`-типы → `*Form` (`api.ts`), `useOrderCockpit` → `useOrderForm`,
  `Cockpit`/`CockpitRow` → `KittingForm`/`KittingFormRow`, тест-классы `*CockpitTests`
  → `*FormTests`, русские «кокпит» в комментариях → «форма». Целевое слово —
  **`form`** (не `*_view`/`*Detail` из первоначальной заявки): совпадает с русским
  словарём `UI_GUIDE` §1, а `view` столкнулось бы с `views.py` и `*View.tsx`.
  Исключение: ключ проекции `cockpit_status` → **`worst_status`** (движок отдаёт
  смысл, а не имя UI-контейнера). Сюита 352 OK, фронт зелёный.

---

## Эксплуатация

*(пусто — пополняется)*
