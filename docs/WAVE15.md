# Волна 15 — план реализации

Живой план-чеклист пятнадцатой волны. Источник «что делаем сейчас и в каком
порядке». Значимые решения по ходу — в [JOURNAL.md](JOURNAL.md) (запись
2026-07-12); здесь — объём, развилки (уже решены на планировании) и этапы.

**Цель волны 15:** справочник `Item` в Plume синхронизирован с внешней
**библиотекой компонентов Altium** (внешний git-репозиторий) — она источник правды по
покупным изделиям. Плюс — разморозка модели: поля `Item` подгоняются под канон
библиотеки, оси «что за изделие» и «производим/покупаем» разведены, стоимость
производимого считается роллапом по BOM.

## Объём

**Входит:**
- Переименования полей `Item` под канон библиотеки (полный охват: модель + БД +
  API + фронт) + новая справочная сущность `Category` + новое поле `temperature`
  + смена оси `is_manufactured→produced` + удаление хранимого `active`
  (→ вычисляемое).
- Форма импорта CSV в режиме «Изделия»: мульти-файл → серверный движок парса
  (CP1251) → диф-вью с чекбоксами → применение подтверждённого (полная сверка:
  добавить / обновить / удалить-или-осиротить).
- Рекурсивный роллап `estimated_cost` по BOM + кнопка «Пересчитать стоимость» в
  форме производимого изделия.

**НЕ входит:** RFQ/КП-сущности; парсинг числовых параметров из `Description`
(тащим как есть, текстом); импорт CAD-полей (`Comment`, `Footprint/Library`); свой
инструмент/скрипт с доступами (отвергнут — путь Б).

## Источник правды по библиотеке

Репозиторий библиотеки (берётся локально). CSV в `csv/*.csv`, **CP1251**,
разделитель `;` (без экранирования), LF, финальный перевод строки. 8 колонок
(схема одинакова у всех таблиц, набор таблиц растёт):

| Колонка | Смысл | → Plume |
|---------|-------|---------|
| `Design Item Id` | уникальный ключ Altium = **заказной** Part Number (с суффиксом упаковки) | `Item.design_item_id` (ключ сопоставления) |
| `Comment` | PN без суффикса, печать на схеме | — (не тащим) |
| `Description` | рус. описание | `Item.description` |
| `Footprint/Library Path/Ref` | CAD-ссылки (часто пустые) | — (не тащим) |
| `Temperature` | предельная T компонента, напр. `-40-125°C` | `Item.temperature` |
| *(имя файла)* | класс компонента: `capacitors`/`mcu`/`regulators`/`sensors`/`interfaces` | `Item.category` (→ `Category`) |

Цену библиотека **не хранит принципиально** — `estimated_cost` остаётся за Plume.

## Решения (утверждены на планировании 2026-07-12)

- **Путь Б** — форма в аппе, не внешний скрипт (юзер уже залогинен, 0 доп.
  инструментов в синхроне).
- **Синк = полная сверка**, каждая ветка под ручной галочкой; scoping «пропавших»
  **по категории** (мульти-файл = вся библиотека за раз).
- **Переименования `Item`** (охват полный, без alias-слоя):
  `code→design_item_id` («Изделие»), `name→description` («Описание»),
  `kind→category` (FK → `Category`, «Категория»). Имя `design_item_id` выбрано
  осознанно вместо `item_id`: `item_id` в рукописном JSON-API уже занят как Django
  FK-PK аксессор (`lot.item_id`) и ключ payload'ов мутаций — коллизия имён.
  `design_item_id` — дословный канон библиотеки (`Design Item Id`), конфликта нет.
  См. JOURNAL 2026-07-12 «Грабли имени `item_id`». verbose_name поля — «Изделие».
  **PK-ссылки (`item_id`/`target_item_id`/`component_id`) при этом НЕ трогаем** —
  они остаются PK и в сериализации, и в payload'ах мутаций.
- **`is_manufactured→produced`** (boolean, «Производимое»; ось ⟂ `category`).
- **`active` — убрано, вычисляемое** («используется/спящий» через `Exists` по
  живым ссылкам). Ветка «пропал»: не используется → удалить; используется →
  флаг «сирота, нет в библиотеке».
- **`kind` = справочник `Category` (FK)**, а не enum/CharField: `code` (стем CSV),
  `label` рус., `icon` Codicon; синк `get_or_create` на лету.
- **Миграции пересобираем в чистый `0001_initial`** (dev + прод). Боевых данных
  нет нигде. **Прод не сеем** — Иван заполнит сам. Dev для дебага сеять можно, но
  **прибирать за собой** (синтетические тестовые названия путают). Иван позже даст
  свой тестовый набор — захардкодим в репозиторий.
- **Стартовые `Category` — только из библиотеки** (5 файлов). Прочие классы
  (прибор/крепёж/деталь/узел…) Иван добавит руками — **не выдумывать**.
- **Роллап стоимости — рекурсивный до листьев.** Сценарий: сменили цену резистора
  → кнопка у прибора → обновилась и стоимость платы (в неё входит резистор), и
  прибора (в него входит плата). Промежуточный производимый узел без собственной
  оценки = сумма состава. Ручная наценка — редко, только перед поставкой; оценка —
  частый инструмент планирования/ведения проекта.

## Источники правды (план ссылается, не дублирует)

- ER-диаграммы (модель БД) → [README.md](../README.md) — **обновить в этой же
  волне** (новая `Category`, поле `temperature`, переименования, `produced`,
  снятое `active`).
- Решения → [JOURNAL.md](JOURNAL.md), запись 2026-07-12.
- Канон форм → [UI_GUIDE.md](UI_GUIDE.md), [FIELD_MATRIX.md](FIELD_MATRIX.md),
  стандарт консистентности форм.

## Этапы

> **Статус реализации (сессия 2026-07-12): РАЗМОРОЗКА МОДЕЛИ ЗАКРЫТА.** Ф0 (схема/
> миграция/сид/admin), Ф3 (вычисляемое used), Ф5-rename (полный фронт + `device→
> produced` + отображение `Category`, сборка зелёная), Ф7 (диаграммы README +
> FIELD_MATRIX) — **сделаны**. Backend 285 тестов зелёные, `0001_initial` пересобран,
> dev-БД пересоздана и засеяна.
>
> **Статус (сессия 2026-07-12, вторая — BACKEND ИМПОРТА ЗАКРЫТ):** Ф1 (парсер
> CP1251/`;`/мульти-файл), Ф2 (диф `new/changed/same/gone/orphan` + применение
> подтверждённых в транзакции), Ф4 (рекурсивный роллап `estimated_cost` до листьев),
> Ф6 (эндпойнты `POST /api/library/diff/`, `POST /api/library/apply/`,
> `POST /api/items/<pk>/recalc-cost/`) — **сделаны**, всё в `engine.py`/`views.py`,
> тонкие вьюхи. **313 тестов зелёные** (285 + 28 новых: parse/diff/apply/rollup/HTTP).
> Verify на реальной библиотеке (`ant-pcb-lib/csv`, 5 файлов, 20 строк): parse →
> diff (все `new`, scoping корректный) → apply в откатанной транзакции (20 создано,
> `produced=false`, категории на лету) — чисто. **Осталось на следующую сессию:**
> фронт (Ф5-форма мульти-CSV + диф-вью с чекбоксами + кнопка «Пересчитать стоимость»)
> и verify импорта через UI. Контракт бэка: диф-строка = `{status, design_item_id,
> item_id, incoming?, current?, changes?}`; apply шлёт те же файлы + `confirmed`
> (JSON-список ключей).

### Ф0 — схема и миграция ✅
- [x] Модель `Category` (`code` uniq, `label`, `icon`; `__str__`, `verbose_name`).
- [x] `Item`: `code→design_item_id` (uniq, verbose_name «Изделие»), `name→description`,
  `kind`(enum)→`category`(FK→`Category`, `PROTECT`); новое `temperature` (`CharField`,
  blank); `is_manufactured→produced`; **удалено `active`**.
- [x] Пересобран `0001_initial` (сквош, [migrations-squash-policy]).
- [x] Сид: `Category` из 5 библиотечных (`ensure_category`) + демо-классы под демо-BOM.
- [x] `admin.py` — регистрация `Category`, ItemAdmin на новые поля.

### Ф1 — парсер библиотеки (чистый Python + тесты) ✅
- [x] Декод CP1251 (`parse_library_file`), split по `;`, нужные колонки по имени
  заголовка (терпимо к порядку/лишним), отсутствие любой из 3 — ошибка.
- [x] Категория из стема имени файла; нормализованные строки `{design_item_id,
  description, temperature, category}`; дубль ключа/пустой ключ/недобор — отказ.
- [x] Мульти-файл (`parse_library`): агрегат по категориям (для scoping), дубль
  категории и дубль ключа между файлами — ошибка.

### Ф2 — движок дифа и применения ✅
- [x] `library_diff`: сверка по `design_item_id`, статусы `new`/`changed`/`same`;
  детектор изменения по `description`/`category`(code)/`temperature`.
- [x] «Пропал» (в БД, класс среди загруженных, нет в загрузке): используется →
  `orphan` (флаг), не используется → `gone` (на удаление). Scoping по категории.
- [x] `apply_library_diff`: диф пересчитывается на сервере (не доверяем клиенту),
  применяются только `confirmed`-ключи: create (`produced=false`), update,
  `delete_item`; `orphan`/`same` — no-op; `ensure_category` на лету; в транзакции.

### Ф3 — вычисляемое «используется/спящий» ✅
- [x] `engine.item_is_used` (Exists: `BomLine`(component)/`Lot`/`PurchaseLine`/
  `ProjectDemand`/`Kitting`(target)/`ProcurementLine`; зеркалит guard'ы `delete_item`).
  Отдаётся ключом `used` в сериализации `Item` (список + деталь). В диф — при Ф2.

### Ф4 — роллап стоимости (движок + эндпойнт ✅; кнопка — при Ф5)
- [x] `rollup_estimated_cost`/`_rollup_cost`: для `produced` узла `estimated_cost =
  Σ(рекурс(component) × qty)`; лист покупной → его `estimated_cost` (None → 0,
  помечен в `incomplete`). Мемоизация общих поддеревьев + guard циклов (`visiting`).
- [x] Пишет оценку во **все** производимые узлы поддерева (не только вершину);
  возвращает `{estimated_cost, updated, incomplete}`.
- [x] Эндпойнт `POST /api/items/<pk>/recalc-cost/` (только `produced`). Кнопка — Ф5.

### Ф5 — фронт (rename ✅; форма импорта/кнопка стоимости — при Ф1/Ф2/Ф4)
- [x] **Полный rename** во всех вьюхах/`api.ts`. Объект **самого** изделия
  (`ItemRow`/`ItemDetail`): `.code→.design_item_id`, `.name→.description`,
  `.kind→.category`. Ссылочные display-ключи (лоты/строки): `*_code→*_design_item_id`,
  `*_name→*_description` (`item_code→item_design_item_id`, `component_code→
  component_design_item_id`, `target_code→target_design_item_id`, `parent_code→
  parent_design_item_id`). **PK-ключи (`item_id`/`target_id`/`component_id`/`parent_id`)
  и payload'ы мутаций — НЕ трогать** (это PK-ссылки, коллизии больше нет).
- [x] `kind==='device'`→`produced` в DeficitView (пикер цели) и ItemView (`composable`).
- [x] Отображение категории через `Category.label`/`icon` (снята хардкод-карта
  `KIND_RU`/`ITEM_ICON` в App.tsx и ItemView.tsx; форма изделия — пикер категории).
- [x] Индикатор «используется/спящий» (`used`) в шапке формы изделия.
- [ ] Форма загрузки CSV (мульти-файл) + диф-вью с чекбоксами — **при Ф1/Ф2/Ф6**.
- [ ] Кнопка «Пересчитать стоимость» (видна для `produced`) — **при Ф4**.

### Ф6 — эндпойнты импорта ✅
- [x] `POST /api/library/diff/` — мульти-файл `files` → диф (без записи), отдаёт
  `{categories, rows}`. `POST /api/library/apply/` — те же `files` + `confirmed`
  (JSON-список ключей) → сводка `{created, updated, deleted}`. Тонкие вьюхи
  (`_library_files` тянет `request.FILES.getlist`), правила — в движке.

### Ф7 — доки и консистентность (rename-часть ✅)
- [x] Обе mermaid-диаграммы [README.md](../README.md): новая `Category` + связь,
  `temperature`, rename, `produced`, снятое `active`; продуктовый глоссарий.
- [x] [FIELD_MATRIX.md](FIELD_MATRIX.md) A1 (форма изделия) — на новые поля.
- [x] [form-consistency-standard]: rename Item-специфичен, паттерны форм переиспользованы,
  по другим формам (Project/Location code/name) не бьёт.
- [x] verify end-to-end rename: backend 285 тестов, фронт-сборка (tsc), живая
  сериализация нового контракта, HTTP `/api/items/` + `/api/categories/`.
- [ ] verify импорта (загрузка → диф → применение → пересчёт) — **при Ф1/Ф2/Ф4/Ф6**.
